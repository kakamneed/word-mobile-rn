# Mini Program Flutter Parity Feedback Template

Use this template after every parity implementation change.

| Area | Flutter/Rust source | Mini-program source | Difference | Change made |
| --- | --- | --- | --- | --- |
|  | `path:line` `function` | `path:line` `function` |  |  |

Rules:

- Cite function names and line numbers.
- State whether Flutter, Rust/backend, or mini-program owns the business logic.
- If a mini-program behavior is intentionally different because of WeChat runtime constraints, say so explicitly.
- Do not claim a feature is parity-complete while it still uses undocumented mock data.
- Include verification command results below the table.
