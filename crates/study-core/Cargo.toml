[package]
name = "word-study-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Study session domain logic for Word"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }

# Keep the source-level compatibility name while resolving to the pure model package.
# This avoids rewriting the user-owned question builder during the dependency inversion.
word-storage-core = { package = "word-domain-models", path = "../domain-models" }

[dev-dependencies]
tokio-test = { workspace = true }
