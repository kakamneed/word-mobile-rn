[package]
name = "word-domain-wasm"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Thin wasm-bindgen adapter for the Word domain protocol"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
word-domain-protocol = { path = "../domain-protocol" }

[dev-dependencies]
serde_json = { workspace = true }
wasm-bindgen-test = "0.3"

