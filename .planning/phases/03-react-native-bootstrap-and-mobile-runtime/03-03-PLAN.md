---
phase: 03-react-native-bootstrap-and-mobile-runtime
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [apps/mobile/src/App.tsx, apps/mobile/src/screens/startup-error-screen.tsx, crates/app-core/src/bootstrap.rs, crates/app-core/src/today.rs, crates/app-core/src/study.rs, apps/mobile/src/lib/mobile-bridge.ts]
autonomous: true
requirements: [MOB-01, MOB-02, STUD-02]
user_setup: []
must_haves:
  truths:
    - "Runtime failures must block startup with visible recovery states."
    - "Phase 3 is only complete if mobile can launch one real study session."
    - "Bootstrap, settings, today-home, and study launch must all run through the real mobile bridge."
  artifacts:
    - path: "apps/mobile/src/screens/startup-error-screen.tsx"
      provides: "Blocking startup error experience with recovery actions"
      min_lines: 30
    - path: "crates/app-core/src/bootstrap.rs"
      provides: "Shared bootstrap facade used by mobile runtime"
      contains: "blocking"
    - path: "apps/mobile/src/lib/mobile-bridge.ts"
      provides: "Bridge client covering bootstrap, settings, today-home, and study launch"
      contains: "startStudy"
  key_links:
    - from: "apps/mobile/src/App.tsx"
      to: "apps/mobile/src/screens/startup-error-screen.tsx"
      via: "blocking runtime failure handling"
      pattern: "error"
    - from: "apps/mobile/src/lib/mobile-bridge.ts"
      to: "crates/app-core/src/study.rs"
      via: "real study-launch invocation"
      pattern: "study"
---

<objective>
Complete the blocking bootstrap experience and validate the mobile runtime through a real study-launch smoke path.

Purpose: Close Phase 3 with runtime behavior that is both observable to users and strong enough to prove mobile viability beyond simple status reads.
Output: Blocking startup error surface, shared bootstrap/today/study facades wired to mobile bridge, and a runtime validation path that launches one real study session.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md
@.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement blocking startup error handling with explicit recovery actions</name>
  <files>apps/mobile/src/App.tsx, apps/mobile/src/screens/startup-error-screen.tsx, crates/app-core/src/bootstrap.rs</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, ../word-desktop-tauri/apps/desktop/src-tauri/src/services/bootstrap_service.rs, apps/mobile/src/App.tsx, apps/mobile/src/screens/startup-error-screen.tsx, crates/app-core/src/bootstrap.rs</read_first>
  <action>Implement a mobile blocking bootstrap path that mirrors the desktop severity policy for hard runtime failures. `apps/mobile/src/App.tsx` must route failed startup into a dedicated `startup-error-screen.tsx`. The screen must present explicit retry or diagnostics-oriented recovery actions for bootstrap, path, SQLite, or seed failures instead of silently degrading into a partially working shell.</action>
  <verify>Get-Content apps/mobile/src/App.tsx; Get-Content apps/mobile/src/screens/startup-error-screen.tsx</verify>
  <acceptance_criteria>
    - `apps/mobile/src/screens/startup-error-screen.tsx` exists
    - `apps/mobile/src/App.tsx` routes blocking bootstrap failures to the startup error screen
    - startup error UI includes at least one explicit retry or recovery action
  </acceptance_criteria>
  <done>Startup failures are visible, blocking, and actionable instead of hidden behind a degraded shell.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shared bootstrap, today-home, settings, and real study-launch facades into the mobile bridge</name>
  <files>crates/app-core/src/bootstrap.rs, crates/app-core/src/today.rs, crates/app-core/src/study.rs, apps/mobile/src/lib/mobile-bridge.ts</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, ../word-desktop-tauri/apps/desktop/src-tauri/src/services/bootstrap_service.rs, ../word-desktop-tauri/apps/desktop/src-tauri/src/commands/study.rs, apps/mobile/src/lib/mobile-bridge.ts, crates/app-core/src/bootstrap.rs, crates/app-core/src/today.rs, crates/app-core/src/study.rs</read_first>
  <action>Expose and wire shared-core facade entrypoints for bootstrap, settings, today-home, and one real study launch through the mobile bridge. The study path must create or return a real session launch response using the shared runtime and seed-backed data, not a fake placeholder. Keep the UI surface minimal, but ensure the bridge contract covers all four runtime validation surfaces in this phase.</action>
  <verify>Get-Content apps/mobile/src/lib/mobile-bridge.ts; Get-Content crates/app-core/src/study.rs</verify>
  <acceptance_criteria>
    - `apps/mobile/src/lib/mobile-bridge.ts` exports bridge calls for bootstrap, settings, today-home, and study launch
    - `crates/app-core/src/study.rs` exists and includes a study-launch-capable facade entrypoint
    - the mobile runtime path to study launch does not depend on mocked session data
  </acceptance_criteria>
  <done>The mobile runtime validation path now reaches shared bootstrap, today, settings, and one real study-launch flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime smoke validation for cold start persistence and study launch</name>
  <files>apps/mobile/src/App.tsx, apps/mobile/src/lib/mobile-bridge.ts, crates/platform-mobile/src/storage.rs</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, apps/mobile/src/App.tsx, apps/mobile/src/lib/mobile-bridge.ts, crates/platform-mobile/src/storage.rs</read_first>
  <action>Add a repeatable runtime smoke validation path that proves the mobile shell can cold start, open the existing SQLite runtime after restart, read bootstrap/settings/today-home state, and launch one real study session. The validation may use development-only wiring or smoke-test hooks, but it must exercise the real bridge and real runtime storage rather than mocks.</action>
  <verify>Get-Content apps/mobile/src/App.tsx; Get-Content apps/mobile/src/lib/mobile-bridge.ts</verify>
  <acceptance_criteria>
    - project documentation or code-level smoke hooks describe or implement a cold-start runtime validation path
    - the validation path includes bootstrap, settings or today-home read, and one real study launch
    - the validation path uses real runtime storage and bridge calls instead of mocked payloads
  </acceptance_criteria>
  <done>Phase 3 closes with a repeatable proof that the mobile runtime can really boot, persist, and reach a study launch.</done>
</task>

</tasks>

<verification>
- [ ] blocking startup error UI exists
- [ ] mobile bridge covers bootstrap, settings, today-home, and study launch
- [ ] phase includes a repeatable runtime smoke validation path with one real study launch
</verification>

<success_criteria>
- Hard runtime failures are visible and blocking
- The mobile app can cold start, initialize runtime storage, and read shared runtime state
- One real study session can be launched through the mobile runtime path
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-03-SUMMARY.md`
</output>
