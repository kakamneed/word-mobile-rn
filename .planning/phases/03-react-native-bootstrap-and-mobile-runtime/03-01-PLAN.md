---
phase: 03-react-native-bootstrap-and-mobile-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/mobile/package.json, apps/mobile/src/App.tsx, apps/mobile/src/lib/mobile-bridge.ts, crates/platform-mobile/src/lib.rs, crates/platform-mobile/src/bootstrap.rs]
autonomous: true
requirements: [MOB-01, MOB-05]
user_setup: []
must_haves:
  truths:
    - "The mobile app must launch as a real React Native shell, not a mock-only prototype."
    - "The mobile shell must call a real native bridge for runtime-facing operations."
    - "The bridge surface in this phase must be sufficient to support bootstrap, settings, today-home, and later study launch integration."
  artifacts:
    - path: "apps/mobile/src/lib/mobile-bridge.ts"
      provides: "React Native bridge client for real native runtime calls"
      contains: "getBootstrapState"
    - path: "crates/platform-mobile/src/lib.rs"
      provides: "Platform-mobile runtime adapter crate"
      min_lines: 30
    - path: "apps/mobile/src/App.tsx"
      provides: "Mobile shell startup entry"
      contains: "bootstrap"
  key_links:
    - from: "apps/mobile/src/App.tsx"
      to: "apps/mobile/src/lib/mobile-bridge.ts"
      via: "startup bootstrap invocation"
      pattern: "mobile-bridge"
    - from: "apps/mobile/src/lib/mobile-bridge.ts"
      to: "crates/platform-mobile/src/lib.rs"
      via: "native bridge boundary"
      pattern: "bootstrap"
---

<objective>
Create the React Native app shell and a real native bridge baseline that can invoke shared-core-backed mobile runtime operations.

Purpose: Replace any placeholder mobile-shell assumptions with a real RN-to-native integration path.
Output: RN app shell, real bridge client, and mobile platform adapter scaffold for bootstrap/settings/today-home/study-launch entrypoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md
@.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the mobile app shell and startup entry</name>
  <files>apps/mobile/package.json, apps/mobile/src/App.tsx</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, apps/mobile/package.json, apps/mobile/src/App.tsx</read_first>
  <action>Create the baseline React Native application shell under `apps/mobile`, including package metadata, startup entry wiring, and an app-level bootstrap flow. The startup entry must perform a real bootstrap request on launch and branch into loading, ready, or blocking-error states instead of rendering a fake success shell.</action>
  <verify>Get-Content apps/mobile/package.json; Get-Content apps/mobile/src/App.tsx</verify>
  <acceptance_criteria>
    - `apps/mobile/package.json` exists and defines the mobile app workspace
    - `apps/mobile/src/App.tsx` exists and performs a bootstrap request during startup
    - `apps/mobile/src/App.tsx` includes loading and blocking-error startup states instead of assuming bootstrap always succeeds
  </acceptance_criteria>
  <done>The project has a real React Native shell entry that treats bootstrap as a required startup dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Create the real mobile bridge client and native boundary</name>
  <files>apps/mobile/src/lib/mobile-bridge.ts, crates/platform-mobile/src/lib.rs, crates/platform-mobile/src/bootstrap.rs</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, apps/mobile/src/lib/mobile-bridge.ts, crates/platform-mobile/src/lib.rs, crates/platform-mobile/src/bootstrap.rs</read_first>
  <action>Create a real mobile bridge module at `apps/mobile/src/lib/mobile-bridge.ts` and a matching `platform-mobile` Rust adapter crate. The bridge must define concrete calls for bootstrap, settings read, today-home read, and study launch entrypoints, even if some are only partially implemented in this plan. The bridge must not use mocked return payloads as the primary runtime source.</action>
  <verify>Get-Content apps/mobile/src/lib/mobile-bridge.ts; Get-Content crates/platform-mobile/src/lib.rs</verify>
  <acceptance_criteria>
    - `apps/mobile/src/lib/mobile-bridge.ts` exists and exports bootstrap, settings, today-home, and study-launch bridge functions
    - `crates/platform-mobile/src/lib.rs` exists and defines the mobile platform adapter crate
    - bridge code does not hardcode fake success payloads for bootstrap as the primary runtime path
  </acceptance_criteria>
  <done>The RN shell has a real native integration boundary instead of a placeholder JS-only adapter.</done>
</task>

</tasks>

<verification>
- [ ] `apps/mobile/src/App.tsx` exists and performs startup bootstrap
- [ ] `apps/mobile/src/lib/mobile-bridge.ts` exists with real runtime bridge functions
- [ ] `crates/platform-mobile/src/lib.rs` exists as the mobile adapter base
</verification>

<success_criteria>
- The mobile project launches into a real RN shell
- Startup is driven by a native bridge request rather than mocked payloads
- The project has a stable place to attach the mobile runtime implementation in later plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-01-SUMMARY.md`
</output>
