---
phase: 03-react-native-bootstrap-and-mobile-runtime
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [crates/platform-mobile/src/paths.rs, crates/platform-mobile/src/storage.rs, crates/platform-mobile/src/seeds.rs, crates/storage-core/src/lib.rs, apps/mobile/android, apps/mobile/ios]
autonomous: true
requirements: [MOB-01, MOB-04, MOB-05]
user_setup: []
must_haves:
  truths:
    - "The mobile runtime must use sandbox-safe paths for SQLite and local runtime files."
    - "The app package must include formal bundled seed data for offline first launch."
    - "SQLite init, migration, and seed installation must complete deterministically before runtime readiness is declared."
  artifacts:
    - path: "crates/platform-mobile/src/paths.rs"
      provides: "Mobile sandbox path resolution"
      contains: "database"
    - path: "crates/platform-mobile/src/storage.rs"
      provides: "SQLite open/migrate runtime preparation"
      contains: "initialize"
    - path: "crates/platform-mobile/src/seeds.rs"
      provides: "Formal bundled seed install or validation flow"
      contains: "seed"
  key_links:
    - from: "crates/platform-mobile/src/storage.rs"
      to: "crates/platform-mobile/src/paths.rs"
      via: "sandbox path resolution"
      pattern: "path"
    - from: "crates/platform-mobile/src/seeds.rs"
      to: "crates/platform-mobile/src/storage.rs"
      via: "seed install before runtime readiness"
      pattern: "seed"
---

<objective>
Implement the mobile runtime foundation for sandbox paths, SQLite lifecycle, and formal bundled seed availability.

Purpose: Make the mobile shell truly offline-capable by preparing the on-device runtime correctly before the app reports readiness.
Output: Mobile path adapter, SQLite initialization/migration flow, formal bundled seed packaging and install logic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md
@.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement mobile sandbox path and runtime storage preparation</name>
  <files>crates/platform-mobile/src/paths.rs, crates/platform-mobile/src/storage.rs, crates/storage-core/src/lib.rs</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, ../word-desktop-tauri/apps/desktop/src-tauri/src/persistence/app_paths.rs, ../word-desktop-tauri/apps/desktop/src-tauri/src/services/runtime_storage_service.rs, crates/platform-mobile/src/paths.rs, crates/platform-mobile/src/storage.rs, crates/storage-core/src/lib.rs</read_first>
  <action>Implement `platform-mobile` runtime helpers that resolve sandbox-safe paths for the database, config, logs, vocab cache, and bundled resources. Add runtime storage preparation that opens or migrates SQLite before runtime readiness is declared. Reuse storage-core initialization logic where possible, but replace all direct Tauri path assumptions with mobile-specific path resolution.</action>
  <verify>Get-Content crates/platform-mobile/src/paths.rs; Get-Content crates/platform-mobile/src/storage.rs</verify>
  <acceptance_criteria>
    - `crates/platform-mobile/src/paths.rs` exists and exposes concrete database and runtime directory resolution helpers
    - `crates/platform-mobile/src/storage.rs` exists and contains SQLite initialization or migration preparation logic
    - no mobile runtime preparation code depends directly on Tauri `AppHandle` path APIs
  </acceptance_criteria>
  <done>The mobile runtime can resolve sandbox-safe storage paths and prepare SQLite deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Package and install formal bundled seed resources for offline startup</name>
  <files>crates/platform-mobile/src/seeds.rs, apps/mobile/android, apps/mobile/ios</files>
  <read_first>.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-CONTEXT.md, .planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-RESEARCH.md, ../word-desktop-tauri/apps/desktop/src-tauri/src/services/local_seed_service.rs, ../word-desktop-tauri/apps/desktop/src-tauri/src/services/snapshot_service.rs, crates/platform-mobile/src/seeds.rs, apps/mobile/android, apps/mobile/ios</read_first>
  <action>Package the formal bundled seed baseline into the mobile app and implement seed validation/install helpers in `crates/platform-mobile/src/seeds.rs`. The phase must use formal bundled seed data rather than a tiny repair payload. Wire the mobile asset layout in both iOS and Android project structures so the runtime can validate and expose formal seed content during offline bootstrap.</action>
  <verify>Get-Content crates/platform-mobile/src/seeds.rs</verify>
  <acceptance_criteria>
    - `crates/platform-mobile/src/seeds.rs` exists and contains formal seed validation or install logic
    - mobile project asset configuration includes bundled seed resources in app packaging for both Android and iOS
    - seed logic does not default to a repair-only five-word-style baseline as the main first-launch path
  </acceptance_criteria>
  <done>The mobile app package contains a formal offline baseline and the runtime knows how to validate or install it.</done>
</task>

</tasks>

<verification>
- [ ] mobile path adapter exists and resolves runtime directories
- [ ] SQLite init/migration exists for mobile runtime
- [ ] formal bundled seed handling exists and is packaged into the mobile app
</verification>

<success_criteria>
- The mobile runtime can initialize SQLite in sandbox-safe locations
- Formal bundled seed resources are available offline on first launch
- Runtime readiness no longer depends on live download success
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-native-bootstrap-and-mobile-runtime/03-02-SUMMARY.md`
</output>
