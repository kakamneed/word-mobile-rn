---
phase: 13
plan: 06
title: Phase 13 verification contracts and DevTools QA
status: planned
requirements_addressed: [MOB-01, MOB-02, STUD-02, STUD-03, STUD-04, STUD-05, PLAN-01, PLAN-02, PLAN-03, RPT-01]
wave: 4
depends_on: [13-02, 13-03, 13-04, 13-05]
---

<objective>
Add regression checks that protect the Phase 13 parity work: report chart math, Croc BTI scoring and weight normalization, plan payload preservation, study feed construction, no-AI mini program UI, and WeChat build stability.
</objective>

<read_first>
- `.planning/phases/13-wechat-miniprogram-flutter-behavior-and-interaction-parity/13-CONTEXT.md`
- `.planning/phases/13-wechat-miniprogram-flutter-behavior-and-interaction-parity/13-FLUTTER-DECOMPOSITION.md`
- `apps/wechat_miniprogram/package.json`
- `apps/wechat_miniprogram/scripts/ui-contract.ts`
- `apps/wechat_miniprogram/scripts/cloud-study-payload-check.mjs`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts`
- `apps/wechat_miniprogram/src/sdk/studyFlow.ts`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx`
- `apps/wechat_miniprogram/src/pages/study/index.tsx`
</read_first>

<tasks>
<task id="13-06-01">
<title>Add report chart contract checks</title>
<action>
Add a focused script or test that verifies:
- report chart constants use `56`, `34`, `18`, `12`, and `22`
- accuracy color thresholds return red below 50, orange below 85, green at 85 or above
- a three-point chart produces increasing x values and y values clamped inside the chart height
- `ReportsOverview` mock data includes `modeSeries`
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/package.json` contains `test:reports`.
- The report test file contains `REPORT_CHART_STEP_X`.
- The report test file contains `modeSeries`.
- `npm.cmd run test:reports` exits 0.
</acceptance_criteria>
</task>

<task id="13-06-02">
<title>Strengthen Croc BTI contract checks</title>
<action>
Update `test:croc-bti` so it verifies:
- all canonical question ids are required for completion
- answers are clamped to the accepted range
- `newWord` and `rootAffix` are excluded from question-type customization
- each mode's question-type weights sum to 100
- `cnToEnInput` is absent
- daily minutes create `totalQuestions = minutes * 4`
- `newWordsPerDay` is a multiple of four
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/package.json` contains `test:croc-bti`.
- The Croc BTI test file contains `cnToEnInput`.
- The Croc BTI test file contains `newWordsPerDay`.
- The Croc BTI test file contains `toBe(100)` or equivalent sum assertion.
- `npm.cmd run test:croc-bti` exits 0.
</acceptance_criteria>
</task>

<task id="13-06-03">
<title>Add plan and study flow contract checks</title>
<action>
Add or update tests/scripts so they verify:
- Plan payload preserves `questionTypeWeightsByMode`
- new-word count rounds down to a multiple of four
- Apply Today calls the apply route after save
- Study start passes mode-specific question-type weights
- Study feed helper appends answered, current, and completion items in the same order as Flutter
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/package.json` contains `test:plan-flow` or an equivalent plan test script.
- `apps/wechat_miniprogram/package.json` contains `test:study-flow`.
- The study-flow test file contains `completion`.
- The study-flow test file contains `questionTypeWeights`.
- `npm.cmd run test:study-flow` exits 0.
</acceptance_criteria>
</task>

<task id="13-06-04">
<title>Update UI contract for no-AI and drawer-only surfaces</title>
<action>
Update `scripts/ui-contract.ts` to verify:
- no AI tab in bottom navigation
- no AI generation page linked from app config
- no Today AI summary card
- Account is not a bottom tab
- Croc BTI and leaderboard are reachable from the drawer
- Reports page contains `modeSeries`
- Study page contains vertical feed implementation markers
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/scripts/ui-contract.ts` contains `modeSeries`.
- `apps/wechat_miniprogram/scripts/ui-contract.ts` contains `croc-bti`.
- `apps/wechat_miniprogram/scripts/ui-contract.ts` contains `leaderboard`.
- `apps/wechat_miniprogram/scripts/ui-contract.ts` contains `AI` in a negative/no-AI assertion.
- `npm.cmd run test:ui-contract` exits 0.
</acceptance_criteria>
</task>

<task id="13-06-05">
<title>Run final WeChat build and manual QA checklist</title>
<action>
Run the final verification command set and record any failures in the phase execution notes:
- `npm.cmd run typecheck`
- `npm.cmd run test:ui-contract`
- `npm.cmd run test:croc-bti`
- `npm.cmd run test:study-flow`
- `npm.cmd run build:weapp`
Then manually open the built mini program in WeChat DevTools and verify:
- Today task buttons enter Study with the intended mode
- Plan save/apply button states change
- Reports daily chart point tap changes selected day
- Reports mode card tap expands compact chart
- Croc BTI can answer all questions, edit result weights, and apply plan
- Study can answer vertically, retain answered pages, and show completion
- Console does not show `TypeError: n[e] is not a function`
- Console does not show `Error: timeout`
</action>
<acceptance_criteria>
- `npm.cmd run typecheck` exits 0.
- `npm.cmd run test:ui-contract` exits 0.
- `npm.cmd run test:croc-bti` exits 0.
- `npm.cmd run test:study-flow` exits 0.
- `npm.cmd run build:weapp` exits 0.
- Phase execution notes contain `TypeError: n[e] is not a function` marked absent or fixed.
- Phase execution notes contain `Error: timeout` marked absent or fixed.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Full command set listed in task 13-06-05.
- Manual WeChat DevTools smoke pass on the generated `dist`.
</verification>

<must_haves>
- Verification must cover data contracts and UI contract, not only TypeScript compilation.
- AI-related UI must remain absent.
- The WeChat runtime blank-page regression must be checked explicitly.
</must_haves>
