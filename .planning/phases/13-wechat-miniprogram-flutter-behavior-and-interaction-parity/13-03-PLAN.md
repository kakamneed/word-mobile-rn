---
phase: 13
plan: 03
title: Croc BTI question, result, plan, and weight parity
status: planned
requirements_addressed: [PLAN-01, PLAN-02, PLAN-03, STUD-02, STUD-03]
wave: 2
depends_on: [13-01]
---

<objective>
Replace the simplified mini Croc BTI with the Flutter-derived behavior: canonical questions, saved answers/minutes, scoring, result editing, daily-minute plan allocation, question-type weights by mode, profile save, plan save, and apply-to-today.
</objective>

<read_first>
- `.planning/phases/13-wechat-miniprogram-flutter-behavior-and-interaction-parity/13-FLUTTER-DECOMPOSITION.md`
- `apps/flutter_mobile/lib/features/croc_bti_screen.dart`
- `apps/flutter_mobile/lib/features/croc_bti_model.dart`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.scss`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts`
- `apps/wechat_miniprogram/src/sdk/types.ts`
- `apps/wechat_miniprogram/src/sdk/client.ts`
- `apps/wechat_miniprogram/src/sdk/httpClient.ts`
</read_first>

<tasks>
<task id="13-03-01">
<title>Align Croc BTI model and question-type names</title>
<action>
Update `apps/wechat_miniprogram/src/sdk/crocBti.ts` so it mirrors the Flutter model shape:
- canonical `crocBtiQuestions` ids and axes from Flutter
- answer values clamped to the Flutter range
- `evaluateCrocBti(answers)` returns `code`, `title`, `summary`, `advice`, `axisScores`, `weights`, `planInput`, and `questionTypeWeightsByMode`
- `normalizeCrocBtiQuestionTypeWeightsByMode()` excludes `newWord` and `rootAffix`
- remove `cnToEnInput`; use valid question type keys including `enToCnInput`, `exampleToCnChoiceNoTranslation`, `enToCnChoice`, `cnToEnChoice`, `wordSkeletonInput`, and `exampleToCnChoice`
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains `normalizeCrocBtiQuestionTypeWeightsByMode`.
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` does not contain `cnToEnInput`.
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains `wordSkeletonInput`.
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains `exampleToCnChoiceNoTranslation`.
- `npm.cmd run test:croc-bti` exits 0.
</acceptance_criteria>
</task>

<task id="13-03-02">
<title>Add Croc BTI SDK profile methods</title>
<action>
Extend the mini SDK interfaces and implementations with:
- `sdk.crocBti.getProfile(): Promise<Record<string, unknown> | null>`
- `sdk.crocBti.saveProfile({ profile }: { profile: Record<string, unknown> }): Promise<void>`
Wire mock storage and HTTP client paths without changing Flutter contracts. Store profile fields `resultCode`, `title`, `summary`, `advice`, `answers`, `axisScores`, `weights`, `planInput`, `questionTypeWeightsByMode`, `dailyLearningMinutes`, `growthRuleEnabled`, `source`, `version`, and `evaluatedAt`.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/types.ts` or `apps/wechat_miniprogram/src/sdk/client.ts` contains `getProfile`.
- `apps/wechat_miniprogram/src/sdk/types.ts` or `apps/wechat_miniprogram/src/sdk/client.ts` contains `saveProfile`.
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `dailyLearningMinutes`.
- `apps/wechat_miniprogram/src/sdk/httpClient.ts` contains `crocBti` or the chosen Croc BTI profile endpoint mapping.
- `npm.cmd run typecheck` exits 0.
</acceptance_criteria>
</task>

<task id="13-03-03">
<title>Restore Croc BTI question view</title>
<action>
Update `pages/croc-bti/index.tsx` to match Flutter's first state:
- load saved answers from WeChat storage scoped by user id when available
- load saved daily minutes from WeChat storage, default `40`, clamp `10..240`
- restore profile answers/minutes from `sdk.crocBti.getProfile()` when available
- show result when all canonical questions are answered
- render a daily minutes slider
- render all question cards with 1/2/3 answer buttons or the Flutter-equivalent mapped scale
- enable the result button only when complete
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `dailyLearningMinutes`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `Taro.getStorage` or `Taro.getStorageSync`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `hasCompleteCrocBtiAnswers`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `evaluateCrocBti`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `Slider` or a WeChat slider component.
</acceptance_criteria>
</task>

<task id="13-03-04">
<title>Restore Croc BTI result tuning and apply side effects</title>
<action>
Update the Croc BTI result state so users can:
- edit daily minutes and recalculate plan input with `crocBtiPlanInputForDailyMinutes`
- edit plan counts with clamping `0..240`
- toggle growth rule enabled
- edit question-type weights for `review`, `mixedTest`, and `wrongWordReinforcement`
- rebalance edited question-type weights back to `100`
- apply result by saving profile, saving plan, applying saved plan to today, and flushing sync when the SDK has `flushPendingToCloud`
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `crocBtiPlanInputForDailyMinutes`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `questionTypeWeightsByMode`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `saveProfile`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `savePlan`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `applySavedPlanToToday`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `flushPendingToCloud` or a guarded optional call.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd run typecheck`
- `npm.cmd run build:weapp`
- `npm.cmd run test:croc-bti`
</verification>

<must_haves>
- Croc BTI cannot use the simplified English-only mini question set after this plan.
- Question-type weights must normalize to 100 per mode.
- Applying Croc BTI must mutate the real plan path used by Today and Study.
</must_haves>
