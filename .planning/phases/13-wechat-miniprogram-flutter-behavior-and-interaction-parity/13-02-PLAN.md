---
phase: 13
plan: 02
title: Reports chart data and interaction parity
status: planned
requirements_addressed: [RPT-01, MOB-02]
wave: 1
depends_on: [13-01]
---

<objective>
Restore Reports to Flutter behavior: backend-backed daily and mode series, tappable daily line chart, selected-day detail, expandable mode cards, compact mode charts, and mode-specific colors.
</objective>

<read_first>
- `.planning/phases/13-wechat-miniprogram-flutter-behavior-and-interaction-parity/13-FLUTTER-DECOMPOSITION.md`
- `apps/flutter_mobile/lib/features/reports_screen.dart`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx`
- `apps/wechat_miniprogram/src/pages/reports/index.scss`
- `apps/wechat_miniprogram/src/sdk/types.ts`
- `apps/wechat_miniprogram/src/sdk/client.ts`
- `apps/wechat_miniprogram/src/sdk/httpClient.ts`
- `apps/wechat_miniprogram/src/sdk/mockData.ts`
</read_first>

<tasks>
<task id="13-02-01">
<title>Extend ReportsOverview contract</title>
<action>
Update `ReportsOverview` in `apps/wechat_miniprogram/src/sdk/types.ts` to include:
- `dailySeries: Array<{ date: string; totalQuestions?: number; questionsAnswered?: number; correctCount?: number; accuracyPercent?: number; accuracy?: number; studyTimeMs?: number; totalTimeMs?: number }>`
- `modeBreakdown: Array<{ mode: StudyMode; totalQuestions?: number; questionsAnswered?: number; correctCount?: number; accuracyPercent?: number; accuracy?: number }>`
- `modeSeries: Partial<Record<StudyMode, Array<{ date: string; totalQuestions?: number; questionsAnswered?: number; correctCount?: number; accuracyPercent?: number; accuracy?: number; studyTimeMs?: number; totalTimeMs?: number }>>>`
Update mock and HTTP decoding so `modeSeries` survives from backend or is an empty object when absent.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `modeSeries`.
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `accuracyPercent`.
- `apps/wechat_miniprogram/src/sdk/client.ts` or `apps/wechat_miniprogram/src/sdk/mockData.ts` contains `modeSeries`.
- `apps/wechat_miniprogram/src/sdk/httpClient.ts` contains `modeSeries`.
- `npm.cmd run typecheck` exits 0.
</acceptance_criteria>
</task>

<task id="13-02-02">
<title>Create report chart point helper</title>
<action>
Add a pure TS helper for report chart point calculation. It must export constants equivalent to Flutter:
- `REPORT_CHART_STEP_X = 56`
- `REPORT_CHART_LEFT_GUTTER = 34`
- `REPORT_CHART_RIGHT_GUTTER = 18`
- `REPORT_CHART_TOP_GUTTER = 12`
- `REPORT_CHART_BOTTOM_GUTTER = 22`
- regular height `140`
- compact height `104`
The helper must return `{ item, x, y, accuracy, color }` for each point. Color thresholds must be `#ff3b30` for `<50`, `#ff9500` for `<85`, and `#34c759` otherwise.
</action>
<acceptance_criteria>
- A file under `apps/wechat_miniprogram/src` contains `REPORT_CHART_STEP_X = 56`.
- The same helper contains `REPORT_CHART_LEFT_GUTTER = 34`.
- The same helper contains `#ff3b30`.
- The same helper contains `#ff9500`.
- The same helper contains `#34c759`.
</acceptance_criteria>
</task>

<task id="13-02-03">
<title>Replace fake bars with tappable line chart</title>
<action>
Update `pages/reports/index.tsx` to render a horizontal-scroll line chart based on the chart helper. It must:
- use `dailySeries`
- select the last day by default
- update selected day when a point is tapped
- show date, total question count, correct count, accuracy, and study time in the selected-day card
- use actual point labels from date suffix and accuracy percentage
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `selectedDay`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `dailySeries`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `onClick` or `onTap` for report point selection.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `studyTimeMs` or `totalTimeMs`.
- `npm.cmd run test:ui-contract` exits 0.
</acceptance_criteria>
</task>

<task id="13-02-04">
<title>Implement expandable mode cards with compact charts</title>
<action>
Update Reports mode section so each `modeBreakdown` entry renders as a tappable card with the Flutter color mapping:
- `newWord: #34c759`
- `review: #007aff`
- `mixedTest: #ff9500`
- `wrongWordReinforcement: #ff3b30`
- `rootAffix: #8e44ad`
The selected card must show advice text and a compact chart from `reports.modeSeries[mode]`. Tapping the selected mode again collapses it.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `selectedMode`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `modeSeries`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `#8e44ad`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `wrongWordReinforcement`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains a collapse path that sets selected mode to `null` or `undefined`.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd run typecheck`
- `npm.cmd run build:weapp`
- Add or update a focused report chart helper test and run it with the repo's npm test command if a test runner already exists.
</verification>

<must_haves>
- Reports charts must be data-backed, not decorative bars.
- Mode compact charts must use `modeSeries[mode]`.
- Missing report data must render empty states instead of throwing.
</must_haves>
