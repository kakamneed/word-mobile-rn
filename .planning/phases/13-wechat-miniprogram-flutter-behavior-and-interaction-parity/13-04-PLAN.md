---
phase: 13
plan: 04
title: Plan editor real save/apply/wordbook/growth behavior
status: planned
requirements_addressed: [PLAN-01, PLAN-02, PLAN-03, MOB-02]
wave: 2
depends_on: [13-01, 13-03]
---

<objective>
Turn the mini Plan page from a visual approximation into the Flutter-derived editor: hydrated fields, dirty state, wordbook selection, growth-rule mode, per-mode rules, save plan, apply to today, and preservation of Croc BTI question-type weights.
</objective>

<read_first>
- `.planning/phases/13-wechat-miniprogram-flutter-behavior-and-interaction-parity/13-FLUTTER-DECOMPOSITION.md`
- `apps/flutter_mobile/lib/features/plan_screen.dart`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx`
- `apps/wechat_miniprogram/src/pages/plan/index.scss`
- `apps/wechat_miniprogram/src/sdk/types.ts`
- `apps/wechat_miniprogram/src/sdk/client.ts`
- `apps/wechat_miniprogram/src/sdk/httpClient.ts`
- `apps/wechat_miniprogram/src/sdk/mockData.ts`
</read_first>

<tasks>
<task id="13-04-01">
<title>Extend plan and wordbook DTOs</title>
<action>
Update mini SDK DTOs to include the Flutter Plan fields:
- `growthRuleMode: 'shared' | 'perMode' | string`
- `growthIntervalDays: number`
- `growthIncrement: number`
- `sharedGrowthRule?: { intervalDays: number; increment: number }`
- `growthRulesByMode?: Record<string, { intervalDays: number; increment: number }>`
- existing `questionTypeWeightsByMode?: Record<string, Record<string, number>>`
Add or verify `WordbookSummary` has `id`, `name`, `totalEntries`, `category`, and `isActive`.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `growthRuleMode`.
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `growthRulesByMode`.
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `sharedGrowthRule`.
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `WordbookSummary`.
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `isActive`.
</acceptance_criteria>
</task>

<task id="13-04-02">
<title>Add plan SDK methods</title>
<action>
Extend mini SDK plan methods with:
- `getWordbooks(): Promise<WordbookSummary[]>`
- `toggleWordbook({ wordbookId, isActive }: { wordbookId: number; isActive: boolean }): Promise<void>`
- `savePlan({ planId, input } or equivalent existing signature): Promise<PlanSummary>`
- `applySavedPlanToToday(): Promise<PlanSummary>`
Wire mock client state and HTTP client calls. Do not alter Flutter-side endpoints or backend semantics.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `getWordbooks`.
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `toggleWordbook`.
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `savePlan`.
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `applySavedPlanToToday`.
- `apps/wechat_miniprogram/src/sdk/httpClient.ts` contains `applySavedPlanToToday` or the exact backend apply route mapping.
</acceptance_criteria>
</task>

<task id="13-04-03">
<title>Implement Flutter dirty state and save payload</title>
<action>
Update `pages/plan/index.tsx` to hydrate local state from `sdk.plan.getActivePlan()` and `sdk.plan.getWordbooks()`. Implement dirty detection for name, all count fields, growth-rule enabled, growth-rule mode, wordbook selection, shared growth interval/increment, and per-mode growth interval/increment. Implement `buildPlanInput()` returning exactly these keys:
- `name`
- `newWordsPerDay`
- `reviewWordsPerDay`
- `mixedTestPerDay`
- `wrongWordTestPerDay`
- `rootAffixPerDay`
- `growthRuleEnabled`
- `growthRuleMode`
- `growthIntervalDays`
- `growthIncrement`
- `sharedGrowthRule`
- `growthRulesByMode`
- `questionTypeWeightsByMode`
Clamp fields like Flutter: new words `0..180` and rounded down to a multiple of four; review `0..200`; mixed `0..50`; wrong `0..30`; root-affix `0..50`; interval `1..365`; increment `0..100`.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `buildPlanInput`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `questionTypeWeightsByMode`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `growthRuleMode`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `growthRulesByMode`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `% 4` or an equivalent new-word multiple-of-four calculation.
</acceptance_criteria>
</task>

<task id="13-04-04">
<title>Wire save, apply, and wordbook interactions</title>
<action>
Update Plan UI buttons and controls:
- wordbook radio/list changes pending wordbook state
- Save persists changed wordbook first, then calls `savePlan`
- Apply Today persists changed wordbook, saves the plan, then calls `applySavedPlanToToday`
- buttons show loading/disabled states while saving/applying
- unsaved-change state controls visual hints and disabled buttons
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `saving`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `applying`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `toggleWordbook`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `applySavedPlanToToday`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `hasUnsavedChanges`.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd run typecheck`
- `npm.cmd run build:weapp`
- Add a focused plan payload test if the repo has an existing JS test runner.
</verification>

<must_haves>
- Plan save must not drop Croc BTI `questionTypeWeightsByMode`.
- Apply Today must be a distinct action after save.
- Wordbook choice must persist through the SDK, not only local UI state.
</must_haves>
