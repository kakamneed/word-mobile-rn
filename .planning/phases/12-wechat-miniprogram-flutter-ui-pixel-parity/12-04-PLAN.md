---
phase: 12
plan: 12-04
title: Restore Wrong Words and Reports surfaces
wave: 3
autonomous: true
requirements:
  - WRNG-02
  - RPT-01
  - STUD-05
---

# 12-04 - Restore Wrong Words and Reports surfaces

## Objective

Rebuild Wrong Words and Reports so they match Flutter screenshots #8 and #10 instead of the current English placeholder cards.

## Read First

- `.planning/phases/12-wechat-miniprogram-flutter-ui-pixel-parity/12-UI-SPEC.md`
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx`
- `apps/wechat_miniprogram/src/pages/wrong-words/index.scss`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx`
- `apps/wechat_miniprogram/src/pages/reports/index.scss`
- Wrong-word and report API/client files used by these pages

## Tasks

1. Rebuild Wrong Words:
   - title `错词本`
   - purple summary card and four metric chips
   - `筛选` card with chip buttons
   - `强化入口` card with lavender pill action
   - `错词列表` card with row layout, phonetic, meaning, meta, score pill, and risk dot
2. Remove English wrong-word labels and dense overlapping text from the current page.
3. Rebuild Reports:
   - title `报告`
   - purple streak hero `0 天连续学习`
   - four metric cards with the Flutter selected/disabled visual treatment
   - `每日正确率趋势` line-chart card with selected-day detail
   - `按模式查看` card group with expanded selected mode and compact mode tiles
4. Implement chart drawing with mini-program-safe primitives: view-based SVG/canvas/custom component is acceptable, but it must render as a line trend rather than the current vertical bars.
5. Preserve report/wrong-word values from the existing backend payloads and map them into Flutter-like presentation models.

## Success Criteria

- Wrong Words screenshot matches Flutter #8 in card order, purple summary, filters, reinforcement button, and word tiles.
- Reports screenshot matches Flutter #10 in streak card, metric grid, line trend chart, mode cards, and Chinese copy.
- No visible text overlaps at 390px logical width.
- Wrong-word strengthening and report drilldown actions keep using existing routes/API clients.
