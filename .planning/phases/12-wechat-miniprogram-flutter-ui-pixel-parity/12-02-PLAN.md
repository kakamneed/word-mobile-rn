---
phase: 12
plan: 12-02
title: Restore Today, reward, diagnostics, and task-breakdown visuals while omitting AI UI
wave: 2
autonomous: true
requirements:
  - STUD-01
  - STUD-05
  - PLAN-01
---

# 12-02 - Restore Today, reward, diagnostics, and task-breakdown visuals while omitting AI UI

## Objective

Rebuild the Today page to match Flutter screenshots #2 and the non-AI parts of #4 while continuing to source counts and actions from the current mini program data layer.

## Read First

- `.planning/phases/12-wechat-miniprogram-flutter-ui-pixel-parity/12-UI-SPEC.md`
- `apps/wechat_miniprogram/src/pages/today/index.tsx`
- `apps/wechat_miniprogram/src/pages/today/index.scss`
- Current Today service/API files used by the page

## Tasks

1. Replace the English Today header and green hero with the Flutter Chinese hero:
   - `学习新词`
   - date
   - `先把今天的新词任务推进起来。`
   - progress rail
   - completion and remaining-unit copy
   - white pill `进入新词学习`
2. Replace the current metric grid/Next cards with `今日任务拆解`.
3. Render four task rows with Flutter labels, subtitles, color dots, counts, progress rails, and chevrons.
4. Add the lower Today sections:
   - `今日奖励`
   - `开发诊断`
5. Recreate the reward card composition with a slot-machine style illustration area and locked gradient reward card.
6. Omit the Flutter AI summary card entirely for mini program v1. Do not show AI placeholders, AI disabled buttons, or AI teaser copy.
7. Remove old English placeholder copy such as `Keep the study loop moving`, `Next`, `Reward`, `Claimed`, and text-only action rows.
8. Verify content does not overlap at 390px logical width and clears the custom tab bar.

## Success Criteria

- Today screenshots visually line up with Flutter #2 and #4 in page order, card count, typography scale, and palette.
- The page contains `今日`, `学习新词`, `今日任务拆解`, `今日奖励`, and `开发诊断`.
- The page does not contain `AI 短文总结`, `生成 AI 短文`, or any AI entry point.
- The page no longer contains the old English Today card copy.
- Start-study actions still route through the existing backend/study flow rather than local-only mock logic.
