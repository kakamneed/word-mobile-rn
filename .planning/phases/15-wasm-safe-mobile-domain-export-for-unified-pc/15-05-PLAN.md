---
phase: 15-wasm-safe-mobile-domain-export-for-unified-pc
plan: 05
type: execute
wave: 4
depends_on: [15-04]
files_modified:
  - scripts/domain-export/build-package.ps1
  - scripts/domain-export/validate-manifest.mjs
  - scripts/domain-export/run-browser-gates.ps1
  - tests/domain-browser/package.json
  - tests/domain-browser/playwright.config.ts
  - tests/domain-browser/domain-wasm.spec.ts
  - artifacts/domain-wasm/manifest.json
  - fixtures/domain/v1/source-lock.json
  - fixtures/domain/v1/source-lock-promotions.json
  - fixtures/domain/v1/evidence/wave-4.json
  - fixtures/domain/v1/evidence/wave-4-reviewed-diff.json
  - docs/features/learning.md
  - docs/features/_reconciliation.md
  - .planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-UAT.md
autonomous: false
requirements: [ARCH-04, ARCH-05, ARCH-06, ARCH-07]
must_haves:
  truths:
    - "A reproducible optimized package is built from a clean committed worktree and identifies its protocol, schema, exact commit, lock hash, artifact hashes, and measured size."
    - "Chromium, Firefox, and WebKit load the generated package and execute canonical requests with structured results/errors."
    - "Current Flutter/mobile Rust regressions pass and release-device NewWord, Review, resume, Today, Wrong Words, and Reports behavior is accepted before completion."
  artifacts:
    - path: "artifacts/domain-wasm/manifest.json"
      provides: "Pin-ready protocol/source/build/artifact identity"
    - path: "tests/domain-browser/domain-wasm.spec.ts"
      provides: "Real three-browser execution and measurement gate"
    - path: ".planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-UAT.md"
      provides: "Recorded automated and release-device completion evidence"
  key_links:
    - from: "tests/domain-browser/domain-wasm.spec.ts"
      to: "artifacts/domain-wasm/manifest.json"
      via: "manifest-pinned generated package import"
      pattern: "manifest"
    - from: "artifacts/domain-wasm/manifest.json"
      to: "fixtures/domain/v1/source-lock.json"
      via: "source and fixture digest identity"
      pattern: "source"
---

<objective>
Produce the pin-ready WASM package and prove it in real browsers and against the current Flutter/mobile production paths.

Purpose: Finish with consumption evidence rather than treating a successful Rust compile as browser readiness or cross-platform parity.
Output: Reproducible artifact manifest, three-browser evidence, full regression record, and device UAT.
</objective>

<execution_context>
@C:/Users/clf20/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/clf20/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-04-SUMMARY.md
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-VALIDATION.md
@apps/flutter_mobile/test/study_question_display_test.dart
@apps/flutter_mobile/test/today_task_breakdown_test.dart
@apps/flutter_mobile/test/wrong_words_screen_test.dart
@apps/flutter_mobile/test/reports_screen_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the candidate and promote the reviewed Wave 4 source</name>
  <files>scripts/domain-export/build-package.ps1, scripts/domain-export/run-browser-gates.ps1, tests/domain-browser/package.json, tests/domain-browser/playwright.config.ts, tests/domain-browser/domain-wasm.spec.ts, fixtures/domain/v1/source-lock.json, fixtures/domain/v1/source-lock-promotions.json, fixtures/domain/v1/evidence/wave-4.json, fixtures/domain/v1/evidence/wave-4-reviewed-diff.json, docs/features/learning.md, docs/features/_reconciliation.md</files>
  <behavior>
    - The candidate passes native/WASM fixtures, Chromium/Firefox/WebKit, full Rust, and focused Flutter gates before its source digest can be promoted.
    - Promotion records the reviewed Wave 4 diff, successful parity evidence, updated ledger digest, and new accepted digest.
    - The task ends with the promoted source lock and evidence in an atomic commit suitable for clean-worktree build input.
  </behavior>
  <action>Run the Wave 4 source/ledger check and build a clearly marked non-release candidate for testing. Exercise the candidate in all three Playwright engines, run canonical native/WASM parity, full workspace tests, and focused Flutter tests, and write machine-readable evidence plus a reviewed diff. Update both feature ledgers with the exact commands and candidate hashes. Only then promote the source lock and require post-promotion `-Check`. This task's atomic GSD commit must contain the promoted lock, promotion record, evidence, reviewed diff, and ledger updates; the candidate itself is not yet pin-ready.</action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/build-package.ps1 -Candidate; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/run-browser-gates.ps1 -WriteEvidence fixtures/domain/v1/evidence/wave-4.json; cargo test --workspace; D:\flutter\flutter\bin\flutter.bat test apps\flutter_mobile\test\study_question_display_test.dart apps\flutter_mobile\test\today_task_breakdown_test.dart apps\flutter_mobile\test\wrong_words_screen_test.dart apps\flutter_mobile\test\reports_screen_test.dart --no-pub --no-test-assets; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Promote -Wave 4 -ReviewedDiff fixtures/domain/v1/evidence/wave-4-reviewed-diff.json -ParityEvidence fixtures/domain/v1/evidence/wave-4.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Check -Wave 4</automated>
  </verify>
  <done>The candidate passes all cross-target/mobile gates and the reviewed Wave 4 source, ledger, evidence, and new digest are atomically committed for the final clean build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and reverify the pin-ready clean-commit artifact</name>
  <files>scripts/domain-export/validate-manifest.mjs, artifacts/domain-wasm/manifest.json, .planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-UAT.md</files>
  <behavior>
    - Two clean temporary worktrees at Task 1's exact commit produce the same optimized WASM hash and normalized manifest identity.
    - The manifest records `dirty=false`, exact commit, promoted source-lock digest, Cargo.lock hash, tool versions, artifact hashes, and measured size.
    - Chromium, Firefox, and WebKit import the final generated package and match canonical result/error fixtures within locked budgets.
  </behavior>
  <action>After Task 1's atomic commit exists, create two temporary clean git worktrees at that exact commit and build with pinned tool versions. A dirty primary worktree may contain unrelated user work but is never a release input. Require identical optimized payload hashes across the two builds. Emit protocol/schema version, exact commit with `dirty=false`, promoted source-lock digest, Cargo.lock SHA-256, tool versions, target/profile/build command, JS/WASM hashes, and raw/gzip size. Lock measured startup/serialization budgets, run all three browser projects against this final artifact, and record final evidence in `15-UAT.md`.</action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/build-package.ps1 -CleanCommittedWorktree -ReproducibilityCheck; node scripts/domain-export/validate-manifest.mjs artifacts/domain-wasm/manifest.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/run-browser-gates.ps1 -FinalArtifact</automated>
  </verify>
  <done>The final artifact is reproducible from Task 1's clean exact commit, references the promoted digest, and passes all three browser engines and measurement budgets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the unchanged mobile flow on a release device</name>
  <files>.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-UAT.md</files>
  <what-built>WASM-safe extraction with native compatibility adapters, canonical parity, browser evidence, and focused automated mobile regressions.</what-built>
  <action>After every automated gate in Tasks 1-2 passes, pause for release-device verification. Do not mark ARCH-07 or Phase 15 complete from browser/build evidence alone. Record the selected device, fresh build identity, and observed results in `15-UAT.md`.</action>
  <how-to-verify>
    1. Install a fresh release build on the explicitly selected Android/iOS device.
    2. Run NewWord with at least two words and confirm type-major four-question rounds, stable accepted meaning, phonetics/examples, and no pre-submit translation leakage.
    3. Run Review and confirm one question per entry; exit after submitting, re-enter, and confirm resume does not restart at question one.
    4. Confirm Today uses question units, then open Wrong Words and Reports and verify real word identity plus the current local day.
    5. Record device/build identity and pass/fail details in `15-UAT.md`.
  </how-to-verify>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Check -Wave 4; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/run-browser-gates.ps1; cargo test -p word-platform-mobile</automated>
  </verify>
  <done>A fresh selected-device build passes the named NewWord, Review, resume, Today, Wrong Words, and Reports checks, with device/build evidence recorded in `15-UAT.md`.</done>
  <resume-signal>Type "approved" or describe the exact mismatch and mode/question where it occurred.</resume-signal>
</task>

</tasks>

<verification>
Require a current Wave 4 source/ledger check, reproducible manifest, three real browser projects, complete canonical parity, full Rust workspace tests, focused Flutter suites, and recorded release-device acceptance.
</verification>

<success_criteria>
- Word Net can pin and load a source-identifiable package with stable protocol errors.
- Chromium, Firefox, and WebKit execute the actual generated artifact within measured budgets.
- The existing mobile product remains behaviorally accepted; no SQLite or lifecycle concern entered WASM.
</success_criteria>

<output>
After completion, create `.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-05-SUMMARY.md` and finalize `15-UAT.md`.
</output>
