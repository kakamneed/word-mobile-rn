---
phase: 15-wasm-safe-mobile-domain-export-for-unified-pc
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/domain-export/check-source-lock.ps1
  - scripts/domain-export/capture-native-fixtures.ps1
  - scripts/domain-export/compare-semantic-json.mjs
  - fixtures/domain/v1/source-lock.json
  - fixtures/domain/v1/source-lock-promotions.json
  - fixtures/domain/v1/manifest.json
  - fixtures/domain/v1/requests/*.json
  - fixtures/domain/v1/expected/*.json
  - crates/app-core/tests/baseline_runner.rs
  - docs/features/learning.md
autonomous: true
requirements: [ARCH-05, ARCH-06, ARCH-07]
must_haves:
  truths:
    - "The authoritative mobile source and learning ledger are hash-locked before extraction begins."
    - "Canonical native outputs capture the current mobile rules before any model or rule moves."
    - "Every later wave can stop on an unexplained source or ledger change instead of consuming stale truth."
    - "An authorized Phase 15 change becomes the next accepted source only after parity evidence, ledger update, and reviewed-diff promotion are recorded."
  artifacts:
    - path: "fixtures/domain/v1/source-lock.json"
      provides: "Mobile commit plus per-file and ledger SHA-256 baseline"
    - path: "fixtures/domain/v1/expected"
      provides: "Canonical native JSON outputs for all required behavior groups"
    - path: "scripts/domain-export/check-source-lock.ps1"
      provides: "Per-wave source/ledger check and evidence-gated promotion workflow"
    - path: "fixtures/domain/v1/source-lock-promotions.json"
      provides: "Append-only reviewed diff, parity evidence, ledger digest, and newly accepted digest history"
  key_links:
    - from: "scripts/domain-export/capture-native-fixtures.ps1"
      to: "crates/app-core/tests/baseline_runner.rs"
      via: "current native production behavior capture"
      pattern: "baseline_runner"
    - from: "fixtures/domain/v1/source-lock.json"
      to: "docs/features/learning.md"
      via: "recorded ledger hash"
      pattern: "learning.md"
---

<objective>
Freeze the current Flutter-backed Rust behavior and establish the source-freshness and fixture infrastructure that every extraction wave must pass.

Purpose: Prevent Phase 15 from extracting stale or historically drifted behavior and create native golden truth before code moves.
Output: Source lock, canonical v1 fixture corpus, semantic comparator, and ledger evidence.
</objective>

<execution_context>
@C:/Users/clf20/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/clf20/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-CONTEXT.md
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-RESEARCH.md
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-VALIDATION.md
@docs/features/learning.md
@docs/features/_reconciliation.md
@tests/baseline/runners/baseline-runner-spec.md
@crates/app-core/tests/baseline_runner.rs
@crates/study-domain-runner/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the mobile source and ledger hard gate</name>
  <files>scripts/domain-export/check-source-lock.ps1, fixtures/domain/v1/source-lock.json, fixtures/domain/v1/source-lock-promotions.json, docs/features/learning.md</files>
  <behavior>
    - A matching authoritative-file and learning-ledger hash set exits zero and identifies the checked wave.
    - Any changed authoritative source or ledger hash exits nonzero with an explicit changed-file list.
    - Phase 15 generated files are excluded from the authoritative input set; changing product behavior files is never silently accepted.
    - Promotion refuses to accept a new digest until the reviewed diff, successful parity evidence, and updated learning-ledger digest are supplied and verified.
  </behavior>
  <action>Create a PowerShell-native gate that records the current git commit/dirty state and SHA-256 values for the authoritative question builder, session definition/summary, app study facade, reports/wrong-word services, mobile bridge, Flutter study display tests, and `docs/features/learning.md`. Support `-Capture -Wave 0`, `-Check -Wave N`, and `-Promote -Wave N -ReviewedDiff <path> -ParityEvidence <path>`. `-Check` fails on unexplained drift. `-Promote` is the only authorized way to advance the accepted digest: it must validate machine-readable successful parity evidence, verify the learning ledger was updated for the reviewed changes, record old/new digests plus reviewed diff and evidence hashes in append-only `source-lock-promotions.json`, then atomically replace the accepted lock. A post-promotion `-Check` must pass. Initial Wave 0 capture freezes current mobile behavior; promotion never authorizes product-rule correction or use of old desktop behavior.</action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Check -Wave 0</automated>
  </verify>
  <done>The checked-in lock represents current mobile truth, stale input fails closed, and a promotion test proves only reviewed parity-preserving changes with updated ledger evidence can become the next accepted digest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture the canonical native fixture corpus</name>
  <files>scripts/domain-export/capture-native-fixtures.ps1, scripts/domain-export/compare-semantic-json.mjs, fixtures/domain/v1/manifest.json, fixtures/domain/v1/requests/*.json, fixtures/domain/v1/expected/*.json, crates/app-core/tests/baseline_runner.rs</files>
  <behavior>
    - NewWord with at least two words emits exactly four type-major rounds and one stable accepted-meaning set per word.
    - Review and every non-NewWord mode emit one persona-selected question per entry, never the four-question loop.
    - Fixtures cover question-count progress/carry-over, type-specific visibility and feedback-only translations, phonetics/examples/distractors, resume state, real wrong-word identity, and explicit report local day.
    - Semantic comparison ignores JSON object key order but preserves array order and values.
  </behavior>
  <action>Extend the existing serial baseline runner only where needed to expose deterministic inputs (`nowUtc`, `localDay`, explicit session id/ordering seed) and capture production outputs. Add versioned request/expected files and a manifest listing fixture IDs, behavior claims, source-lock digest, and protocol placeholder. The capture command must refuse to overwrite expected outputs unless `-AcceptCurrentMobileTruth` is explicit. Keep translations hidden in pre-submit payloads and include post-submit feedback fixtures separately.</action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/capture-native-fixtures.ps1 -Verify; node scripts/domain-export/compare-semantic-json.mjs --self-test; cargo test -p word-app-core --test baseline_runner -- --test-threads=1</automated>
  </verify>
  <done>All required behavior groups have deterministic native requests and accepted expected JSON tied to the source lock, and existing mobile baseline tests remain green.</done>
</task>

</tasks>

<verification>
Run the source-lock check, fixture verification, semantic comparator self-test, and serial app-core baseline suite. Inspect the manifest to confirm every required behavior group is named.
</verification>

<success_criteria>
- A stale mobile implementation or ledger stops execution before extraction.
- Canonical fixtures encode the locked NewWord/Review and content/progress/resume/wrong/report contracts.
- Fixture acceptance and later source-lock promotions are explicit, evidence-gated, and traceable, never incidental rewrites.
</success_criteria>

<output>
After completion, create `.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-01-SUMMARY.md`.
</output>
