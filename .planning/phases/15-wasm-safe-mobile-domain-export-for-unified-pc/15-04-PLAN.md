---
phase: 15-wasm-safe-mobile-domain-export-for-unified-pc
plan: 04
type: execute
wave: 3
depends_on: [15-03]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/domain-protocol/Cargo.toml
  - crates/domain-protocol/src/lib.rs
  - crates/domain-protocol/src/v1.rs
  - crates/domain-protocol/tests/protocol_v1.rs
  - crates/domain-wasm/Cargo.toml
  - crates/domain-wasm/src/lib.rs
  - crates/domain-wasm/tests/fixture_equivalence.rs
  - crates/study-domain-runner/Cargo.toml
  - crates/study-domain-runner/src/main.rs
  - fixtures/domain/v1/source-lock.json
  - fixtures/domain/v1/source-lock-promotions.json
  - fixtures/domain/v1/evidence/wave-3.json
  - fixtures/domain/v1/evidence/wave-3-reviewed-diff.json
  - docs/features/learning.md
autonomous: true
requirements: [ARCH-05, ARCH-06, ARCH-07]
must_haves:
  truths:
    - "Native CLI and WASM adapter invoke the same versioned dispatcher and produce semantically identical responses."
    - "Unsupported versions, unknown commands, missing required fields, and invalid data return stable structured errors."
    - "Additive unknown fields do not break protocol v1 consumers."
  artifacts:
    - path: "crates/domain-protocol/src/v1.rs"
      provides: "Versioned request/result/error envelopes and one pure dispatcher"
    - path: "crates/domain-wasm/src/lib.rs"
      provides: "Thin wasm-bindgen adapter over the shared dispatcher"
    - path: "crates/study-domain-runner/src/main.rs"
      provides: "Native adapter over the same dispatcher"
  key_links:
    - from: "crates/domain-wasm/src/lib.rs"
      to: "crates/domain-protocol/src/v1.rs"
      via: "execute_v1 passthrough"
      pattern: "execute_v1"
    - from: "crates/study-domain-runner/src/main.rs"
      to: "crates/domain-protocol/src/v1.rs"
      via: "same execute_v1 dispatcher"
      pattern: "execute_v1"
---

<objective>
Publish one additive v1 JSON protocol and connect both native and WASM adapters to the same pure dispatcher.

Purpose: Give Word Net a stable, structured contract without allowing adapters to duplicate learning rules.
Output: Protocol crate, native runner, WASM adapter, and cross-target fixture equivalence tests.
</objective>

<execution_context>
@C:/Users/clf20/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/clf20/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-03-SUMMARY.md
@crates/study-domain-runner/src/main.rs
@fixtures/domain/v1/requests
@fixtures/domain/v1/expected
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test protocol v1 envelopes and errors</name>
  <files>Cargo.toml, Cargo.lock, crates/domain-protocol/Cargo.toml, crates/domain-protocol/src/lib.rs, crates/domain-protocol/src/v1.rs, crates/domain-protocol/tests/protocol_v1.rs</files>
  <behavior>
    - Every response contains protocol version, request id, source identity, and exactly one of result/error.
    - Protocol v1 accepts additive unknown fields but rejects unsupported major versions and missing required fields with stable codes.
    - Every domain command routes to typed domain-core input/output without adapter rule selection.
  </behavior>
  <action>Run the Wave 3 source/ledger check. Implement typed v1 envelopes, command enum, compatibility policy, source identity, and structured error taxonomy. The dispatcher accepts JSON at the boundary but immediately deserializes typed payloads. Do not panic, print errors only to stderr, or expose raw Rust/JS exceptions as the public contract.</action>
  <verify>
    <automated>cargo test -p word-domain-protocol; cargo check -p word-domain-protocol --target wasm32-unknown-unknown</automated>
  </verify>
  <done>Protocol v1 behavior and compatibility/error cases are fully test-covered and compile for native/wasm32.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire thin native and WASM runners to the same dispatcher</name>
  <files>crates/domain-wasm/Cargo.toml, crates/domain-wasm/src/lib.rs, crates/domain-wasm/tests/fixture_equivalence.rs, crates/study-domain-runner/Cargo.toml, crates/study-domain-runner/src/main.rs, fixtures/domain/v1/source-lock.json, fixtures/domain/v1/source-lock-promotions.json, fixtures/domain/v1/evidence/wave-3.json, fixtures/domain/v1/evidence/wave-3-reviewed-diff.json, docs/features/learning.md</files>
  <behavior>
    - Native and WASM responses are semantically identical for every canonical fixture.
    - The WASM dependency graph excludes storage and all platform crates.
    - The existing native runner commands remain available through compatibility mapping while rules execute only in protocol/domain-core.
  </behavior>
  <action>Create a minimal `wasm-bindgen` export that passes JSON to `execute_v1` and returns its JSON response. Refactor the native runner to the same function; keep old CLI command names only as input adapters if downstream tools still use them. Run every canonical request through native and WASM test runners and compare parsed JSON with array order preserved. After equivalence passes, emit Wave 3 evidence, update the learning ledger with the reviewed protocol/adapter diff, promote the source lock with the evidence and ledger digest, and require a post-promotion check.</action>
  <verify>
    <automated>cargo test -p word-domain-wasm -p word-study-domain-runner; cargo check -p word-domain-wasm --target wasm32-unknown-unknown; cargo tree -p word-domain-wasm | Select-String -Pattern 'rusqlite|reqwest|jni|objc|tauri|platform-mobile' -Quiet | ForEach-Object { exit 1 }; wasm-pack test --node crates/domain-wasm; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/capture-native-fixtures.ps1 -Verify -WriteEvidence fixtures/domain/v1/evidence/wave-3.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Promote -Wave 3 -ReviewedDiff fixtures/domain/v1/evidence/wave-3-reviewed-diff.json -ParityEvidence fixtures/domain/v1/evidence/wave-3.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Check -Wave 3</automated>
  </verify>
  <done>All canonical requests pass native/WASM equivalence, the WASM graph is platform/persistence free, and Wave 3's reviewed diff/ledger/evidence/new accepted digest are recorded and current.</done>
</task>

</tasks>

<verification>
Run Wave 3 freshness, protocol compatibility/error tests, native and wasm32 builds, forbidden-dependency scan, and the full canonical cross-target comparator.
</verification>

<success_criteria>
- Word Net can rely on an explicit versioned envelope and stable error codes.
- Native and WASM adapters contain conversion glue only.
- Every required fixture is equivalent across targets.
</success_criteria>

<output>
After completion, create `.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-04-SUMMARY.md`.
</output>
