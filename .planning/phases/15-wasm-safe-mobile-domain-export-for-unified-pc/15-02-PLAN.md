---
phase: 15-wasm-safe-mobile-domain-export-for-unified-pc
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/domain-models/Cargo.toml
  - crates/domain-models/src/lib.rs
  - crates/domain-models/src/study.rs
  - crates/domain-models/src/projections.rs
  - crates/storage-core/Cargo.toml
  - crates/storage-core/src/models/mod.rs
  - crates/storage-core/src/models/study_*.rs
  - crates/storage-core/src/models/today_home_state.rs
  - crates/storage-core/src/models/wrong_word_state.rs
  - crates/study-core/Cargo.toml
  - fixtures/domain/v1/source-lock.json
  - fixtures/domain/v1/source-lock-promotions.json
  - fixtures/domain/v1/evidence/wave-1.json
  - fixtures/domain/v1/evidence/wave-1-reviewed-diff.json
  - docs/features/learning.md
autonomous: true
requirements: [ARCH-04, ARCH-07]
must_haves:
  truths:
    - "Shared DTOs compile for wasm32 without bringing SQLite or any platform runtime into the graph."
    - "Existing storage, app-core, platform-mobile, and Flutter JSON shapes remain compatible during the move."
    - "Wave 1 runs only against the same reviewed mobile baseline and ledger as Wave 0."
  artifacts:
    - path: "crates/domain-models"
      provides: "Serde-only canonical study and projection DTO ownership"
    - path: "crates/storage-core/src/models/mod.rs"
      provides: "Compatibility re-exports for existing native consumers"
  key_links:
    - from: "crates/storage-core/src/models/mod.rs"
      to: "crates/domain-models/src/lib.rs"
      via: "temporary public re-exports"
      pattern: "word_domain_models"
    - from: "crates/study-core/Cargo.toml"
      to: "crates/domain-models/Cargo.toml"
      via: "pure model dependency replacing storage ownership"
      pattern: "word-domain-models"
---

<objective>
Invert the current storage-owned DTO dependency into a serde-only domain model crate while preserving every native/mobile contract.

Purpose: Remove the unconditional `study-core -> storage-core -> rusqlite` path that blocks a legitimate WASM boundary.
Output: WASM-safe canonical DTO crate and compatibility re-exports.
</objective>

<execution_context>
@C:/Users/clf20/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/clf20/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-01-SUMMARY.md
@crates/storage-core/src/models/mod.rs
@crates/storage-core/src/models/study_question.rs
@crates/storage-core/src/models/study_requests.rs
@crates/storage-core/src/models/study_result.rs
@crates/storage-core/src/models/study_session.rs
@crates/study-core/Cargo.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish serde-only model ownership and compatibility tests</name>
  <files>Cargo.toml, Cargo.lock, crates/domain-models/Cargo.toml, crates/domain-models/src/lib.rs, crates/domain-models/src/study.rs, crates/domain-models/src/projections.rs</files>
  <behavior>
    - Existing v1 JSON samples deserialize and reserialize semantically unchanged through canonical model types.
    - Unknown additive fields are accepted where the protocol contract requires forward compatibility.
    - The model crate's dependency graph contains no `rusqlite`, filesystem/network client, Flutter, JNI, Objective-C, or Tauri crate.
  </behavior>
  <action>First run the Wave 1 source/ledger gate. Create `word-domain-models` and move only serde DTOs/enums needed by study, plan/progress, resume, wrong-word, and report calculations. Keep platform clocks, repositories, SQL row conversion, and lifecycle types out. Preserve serialized names/defaults exactly; add fixture-backed serde compatibility tests before moving callers.</action>
  <verify>
    <automated>cargo test -p word-domain-models; cargo tree -p word-domain-models | Select-String -Pattern 'rusqlite|reqwest|jni|objc|tauri' -Quiet | ForEach-Object { exit 1 }; cargo check -p word-domain-models --target wasm32-unknown-unknown</automated>
  </verify>
  <done>Canonical DTOs are serde-compatible with v1 fixtures and the crate builds natively and for wasm32 with a clean dependency graph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-export canonical models without changing mobile call paths</name>
  <files>crates/storage-core/Cargo.toml, crates/storage-core/src/models/mod.rs, crates/storage-core/src/models/study_*.rs, crates/storage-core/src/models/today_home_state.rs, crates/storage-core/src/models/wrong_word_state.rs, crates/study-core/Cargo.toml, fixtures/domain/v1/source-lock.json, fixtures/domain/v1/source-lock-promotions.json, fixtures/domain/v1/evidence/wave-1.json, fixtures/domain/v1/evidence/wave-1-reviewed-diff.json, docs/features/learning.md</files>
  <behavior>
    - Existing `word_storage_core::models::*` imports continue compiling while canonical ownership lives in `domain-models`.
    - Study-core no longer depends on storage-core for canonical DTOs.
    - Current app-core and platform-mobile focused behavior tests produce the same fixture outputs.
  </behavior>
  <action>Make storage-core depend on and temporarily re-export canonical types, retaining persistence-only structs and row/repository concerns locally. Switch study-core to `word-domain-models`; do not rename bridge symbols or Flutter-facing JSON fields. Remove duplicate type definitions only after compile/tests prove callers resolve the same public contracts. Preserve the authoritative behavior frozen in Wave 0 exactly; Phase 15 is not authorized to correct or reinterpret `SessionMode::uses_four_question_loop` or any Review/NewWord rule. After all equivalence gates pass, write machine-readable Wave 1 evidence, update `docs/features/learning.md` with the reviewed model-ownership diff and commands, promote the new accepted digest, and require a post-promotion check.</action>
  <verify>
    <automated>cargo check -p word-storage-core -p word-study-core -p word-app-core -p word-platform-mobile; cargo test -p word-study-core; cargo test -p word-app-core --test baseline_runner -- --test-threads=1; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/capture-native-fixtures.ps1 -Verify -WriteEvidence fixtures/domain/v1/evidence/wave-1.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Promote -Wave 1 -ReviewedDiff fixtures/domain/v1/evidence/wave-1-reviewed-diff.json -ParityEvidence fixtures/domain/v1/evidence/wave-1.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Check -Wave 1</automated>
  </verify>
  <done>Native/mobile consumers compile unchanged, canonical outputs match Wave 0, and the reviewed Wave 1 diff/ledger/evidence/new accepted digest are recorded so the final check passes.</done>
</task>

</tasks>

<verification>
Run the Wave 1 source gate, dependency scans, wasm32 model build, workspace-focused checks, study-core tests, and native fixture verification.
</verification>

<success_criteria>
- `word-domain-models` is genuinely platform/persistence free.
- Native compatibility is preserved through deliberate re-exports rather than duplicated DTOs.
- No current Flutter/Rust contract changes as a side effect of model extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-02-SUMMARY.md`.
</output>
