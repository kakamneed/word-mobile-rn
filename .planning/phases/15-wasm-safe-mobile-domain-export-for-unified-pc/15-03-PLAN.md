---
phase: 15-wasm-safe-mobile-domain-export-for-unified-pc
plan: 03
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - crates/domain-core/Cargo.toml
  - crates/domain-core/src/lib.rs
  - crates/domain-core/src/context.rs
  - crates/domain-core/src/study.rs
  - crates/domain-core/src/progress.rs
  - crates/domain-core/tests/study_fixtures.rs
  - crates/study-core/src/question_builder.rs
  - crates/study-core/src/answer_evaluator.rs
  - crates/study-core/src/session_definition.rs
  - crates/study-core/src/session_summary.rs
  - crates/study-core/src/state_transition.rs
  - crates/domain-core/src/projections.rs
  - crates/domain-core/tests/projection_fixtures.rs
  - crates/app-core/src/services/reports_service.rs
  - crates/app-core/src/services/wrong_words_service.rs
  - crates/app-core/src/facade/study_facade.rs
  - fixtures/domain/v1/source-lock.json
  - fixtures/domain/v1/source-lock-promotions.json
  - fixtures/domain/v1/evidence/wave-2.json
  - fixtures/domain/v1/evidence/wave-2-reviewed-diff.json
  - docs/features/learning.md
autonomous: true
requirements: [ARCH-05, ARCH-07]
must_haves:
  truths:
    - "One deterministic pure implementation owns NewWord/Review generation, answers, progress, summaries, and resume transitions."
    - "NewWord stays type-major with four questions per word while Review remains one selected question per entry."
    - "The existing study-core/mobile path delegates to the extracted logic and remains behaviorally unchanged."
    - "Wrong-word and report projections preserve real word identity, explicit local-day semantics, and question-unit totals without moving SQLite into the pure core."
  artifacts:
    - path: "crates/domain-core/src/study.rs"
      provides: "Deterministic study generation and answer rules"
    - path: "crates/domain-core/tests/study_fixtures.rs"
      provides: "Canonical fixture conformance suite"
    - path: "crates/domain-core/src/projections.rs"
      provides: "Typed deterministic wrong-word and report projections"
  key_links:
    - from: "crates/study-core/src/question_builder.rs"
      to: "crates/domain-core/src/study.rs"
      via: "compatibility facade delegation"
      pattern: "word_domain_core"
    - from: "crates/domain-core/tests/study_fixtures.rs"
      to: "fixtures/domain/v1/expected"
      via: "semantic canonical assertions"
      pattern: "fixtures/domain/v1"
    - from: "crates/app-core/src/services/reports_service.rs"
      to: "crates/domain-core/src/projections.rs"
      via: "typed adapter after native repository reads"
      pattern: "word_domain_core"
---

<objective>
Extract deterministic study, progress, summary, resume, wrong-word, and report rules into the pure domain core while preserving the production mobile facade.

Purpose: Establish one target-neutral learning engine rather than a WASM rewrite or TypeScript duplicate.
Output: Pure study/projection domain functions, canonical fixture tests, and native compatibility adapters.
</objective>

<execution_context>
@C:/Users/clf20/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/clf20/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-02-SUMMARY.md
@crates/study-core/src/question_builder.rs
@crates/study-core/src/answer_evaluator.rs
@crates/study-core/src/session_definition.rs
@crates/study-core/src/session_summary.rs
@crates/study-core/src/state_transition.rs
@crates/app-core/src/services/reports_service.rs
@crates/app-core/src/services/wrong_words_service.rs
@fixtures/domain/v1/manifest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define deterministic domain context and study contracts</name>
  <files>crates/domain-core/Cargo.toml, crates/domain-core/src/lib.rs, crates/domain-core/src/context.rs, crates/domain-core/src/study.rs, crates/domain-core/src/progress.rs, crates/domain-core/tests/study_fixtures.rs</files>
  <behavior>
    - The same explicit `nowUtc`, `localDay`, session id, and ordering seed always produce identical results.
    - Required fixture groups assert question order/count, stable meanings, content visibility, option integrity, question-unit progress, duplicate-submit caps, and resume position/state.
    - No exported calculation calls `Utc::now`, `Local::now`, random generators, filesystem, network, or SQLite.
  </behavior>
  <action>Run the Wave 2 source/ledger check before editing. Create `word-domain-core` with an explicit deterministic execution context and typed pure functions. Write fixture tests first against the Wave 0 corpus, then move the smallest coherent rule bodies. Preserve array order as product behavior and keep example translations feedback-only. Do not introduce `serde_json::Value` as the public domain API.</action>
  <verify>
    <automated>cargo test -p word-domain-core study_fixture -- --nocapture; cargo check -p word-domain-core --target wasm32-unknown-unknown</automated>
  </verify>
  <done>All canonical study/progress/resume fixtures pass through deterministic typed functions on native and wasm32 compilation succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract typed wrong-word and report projections behind native adapters</name>
  <files>crates/domain-core/src/projections.rs, crates/domain-core/tests/projection_fixtures.rs, crates/app-core/src/services/reports_service.rs, crates/app-core/src/services/wrong_words_service.rs, crates/app-core/src/facade/study_facade.rs</files>
  <behavior>
    - Wrong-word results retain non-empty real `entrySourceId` and never synthesize `unknown`.
    - Reports filter zero-answer rows, normalize modes, and use the supplied local day for recency/day filling.
    - Progress and summaries use question counts, deduplicate repeated result identity, and do not rewrite plan targets.
    - SQLite reads/writes, transactions, and session snapshots remain in native app/storage adapters.
  </behavior>
  <action>Define typed projection inputs/outputs and fixture tests, then move reusable calculations out of the current `serde_json::Value` services. Keep repository access in app-core; adapt native query output into pure inputs and convert results back at the existing facade boundary. Replace implicit `Local::now()` only inside exported calculations with the request context. Add direct-domain versus app-service equivalence assertions before removing duplicate branches.</action>
  <verify>
    <automated>cargo test -p word-domain-core projection_fixture -- --nocapture; cargo test -p word-app-core reports_service -- --test-threads=1; cargo test -p word-app-core wrong_words_service -- --test-threads=1; cargo check -p word-domain-core --target wasm32-unknown-unknown; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/capture-native-fixtures.ps1 -Verify</automated>
  </verify>
  <done>Typed pure projections match canonical wrong/report/progress fixtures, native services remain compatible, and no persistence/platform access enters domain-core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn study-core into a compatibility facade over domain-core</name>
  <files>crates/study-core/src/question_builder.rs, crates/study-core/src/answer_evaluator.rs, crates/study-core/src/session_definition.rs, crates/study-core/src/session_summary.rs, crates/study-core/src/state_transition.rs, fixtures/domain/v1/source-lock.json, fixtures/domain/v1/source-lock-promotions.json, fixtures/domain/v1/evidence/wave-2.json, fixtures/domain/v1/evidence/wave-2-reviewed-diff.json, docs/features/learning.md</files>
  <behavior>
    - Existing public study-core calls yield semantic output identical to direct domain-core calls.
    - Existing Flutter-backed app-core start/submit/resume behavior does not change.
    - Review and other non-NewWord modes cannot enter the four-question loop through compatibility helpers.
  </behavior>
  <action>Delegate the existing study-core public surface to domain-core, retaining thin compatibility wrappers only where callers require them. Eliminate copied rule branches; the same pure function must serve future WASM and current native paths. Add side-by-side facade/direct assertions for canonical fixtures before deleting old bodies. After study and projection equivalence gates pass, emit Wave 2 evidence, update the learning ledger with the reviewed extraction diff, promote the source lock with reviewed-diff/parity hashes and the new ledger digest, then require the promoted lock to pass `-Check`.</action>
  <verify>
    <automated>cargo test -p word-study-core; cargo test -p word-app-core study_facade::tests:: -- --test-threads=1; cargo test -p word-app-core --test baseline_runner -- --test-threads=1; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/capture-native-fixtures.ps1 -Verify -WriteEvidence fixtures/domain/v1/evidence/wave-2.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Promote -Wave 2 -ReviewedDiff fixtures/domain/v1/evidence/wave-2-reviewed-diff.json -ParityEvidence fixtures/domain/v1/evidence/wave-2.json; powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/domain-export/check-source-lock.ps1 -Check -Wave 2</automated>
  </verify>
  <done>Study-core is compatibility glue over one pure implementation, locked behavior remains equivalent, and Wave 2's reviewed diff, ledger, evidence, and promoted digest are recorded and current.</done>
</task>

</tasks>

<verification>
Check Wave 2 freshness, study and projection fixture conformance, wasm32 compilation, direct/facade equivalence, and serial app-core regressions.
</verification>

<success_criteria>
- One pure study implementation powers both current mobile and the future WASM adapter.
- All locked study/progress/resume behavior is deterministic and fixture-proven.
- Existing public native/mobile calls remain compatible.
- Wrong/report/progress calculations preserve identity, local-day, and question-unit semantics while SQLite stays native.
</success_criteria>

<output>
After completion, create `.planning/phases/15-wasm-safe-mobile-domain-export-for-unified-pc/15-03-SUMMARY.md`.
</output>
