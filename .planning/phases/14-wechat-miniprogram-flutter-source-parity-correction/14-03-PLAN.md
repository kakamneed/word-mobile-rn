---
phase: 14
plan: 03
title: Correct Croc BTI result/apply flow, Plan coverage, and Today sync semantics
status: planned
requirements_addressed: [MOB-02, PLAN-01, PLAN-02, PLAN-03, STUD-04]
wave: 2
depends_on: [14-01]
---

<objective>
Repair Croc BTI, Plan, and Today state flows so the mini program uses Flutter's canonical questions, profile visuals, plan apply sequence, question-type weights, and daily-progress refresh semantics.
</objective>

<read_first>
- `.planning/phases/14-wechat-miniprogram-flutter-source-parity-correction/14-CONTEXT.md`
- `.planning/phases/14-wechat-miniprogram-flutter-source-parity-correction/14-RESEARCH.md`
- `apps/flutter_mobile/lib/features/croc_bti_screen.dart`
- `apps/flutter_mobile/lib/features/croc_bti_model.dart`
- `apps/flutter_mobile/lib/features/plan_screen.dart`
- `apps/flutter_mobile/lib/sdk/plan_client.dart`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts`
- `apps/wechat_miniprogram/src/sdk/client.ts`
- `apps/wechat_miniprogram/src/sdk/types.ts`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.scss`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx`
- `apps/wechat_miniprogram/src/pages/today/index.tsx`
</read_first>

<tasks>
<task id="14-03-01">
<title>Fix Croc BTI fresh-answer state and result gating</title>
<action>
Ensure Croc BTI answers are absent until the user taps a chip. Do not initialize answers to neutral/default values. Implement `hasCompleteCrocBtiAnswers(answers)` as Flutter does: every canonical `crocBtiQuestions` id must be present. The result button must be disabled with disabled styling until complete and must switch to result view when complete.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains `every((question) =>`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` does not initialize all answers to `0`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `hasCompleteCrocBtiAnswers`.
- `npm.cmd run test:croc-bti` includes or is updated to assert fresh answers are incomplete and contain no default selected question.
</acceptance_criteria>
</task>

<task id="14-03-02">
<title>Correct Croc BTI result card layout and assets</title>
<action>
Update result layout to match Flutter `_ResultView` result card:
- card padding 22px
- image block height 180px, `mode="aspectFit"` or equivalent contain fit
- code as its own primary-colored bold line
- title as its own heavy headline line
- summary centered below title
Use `result.assetPath` from copied Flutter assets under `src/assets/croc_bti`. Do not render code and title inline.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/croc-bti/index.scss` contains `height: 180px`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.scss` contains `text-align: center`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `mode=\"aspectFit\"` or `mode='aspectFit'`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `result.code`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `result.title`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` does not contain `result.code}{result.title`.
</acceptance_criteria>
</task>

<task id="14-03-03">
<title>Port Croc apply sequence and question-type rebalance</title>
<action>
Implement the Flutter `_applyResult()` order in the mini SDK/page:
1. save local answers
2. save local daily minutes
3. build `planInput`
4. build normalized `questionTypeWeightsByMode`
5. call `sdk.crocBti.saveProfile(profile)`
6. call `sdk.plan.savePlan(planId,input)`
7. call `sdk.plan.applySavedPlanToToday()`
8. call `sdk.sync.flushPendingToCloud()`
9. navigate back or refresh Today
Also port `_rebalanceQuestionTypeWeights(current,changedKey,changedValue)` and ensure each edited mode totals 100 after a slider/input change.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` or `apps/wechat_miniprogram/src/sdk/client.ts` contains `saveProfile`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` or `apps/wechat_miniprogram/src/sdk/client.ts` contains `savePlan`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` or `apps/wechat_miniprogram/src/sdk/client.ts` contains `applySavedPlanToToday`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` or `apps/wechat_miniprogram/src/sdk/client.ts` contains `flushPendingToCloud`.
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains `rebalanceQuestionTypeWeights`.
- `npm.cmd run test:croc-bti` includes or is updated to assert a rebalanced mode sums to 100.
- `npm.cmd run test:croc-bti` includes or is updated to assert apply call order.
</acceptance_criteria>
</task>

<task id="14-03-04">
<title>Preserve Plan fields and Today refresh semantics</title>
<action>
Update Plan save/apply code so it preserves these Flutter fields when editing:
- `questionTypeWeightsByMode`
- `growthRuleMode`
- `sharedGrowthRule`
- `growthRulesByMode`
- selected active wordbook via `toggleWordbook`
Update Today so after plan apply or completed study session it reloads from `sdk.today.getHomeState()` or equivalent SDK state instead of trusting stale local values. Do not add AI UI.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `questionTypeWeightsByMode`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `growthRulesByMode`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `toggleWordbook`.
- `apps/wechat_miniprogram/src/pages/today/index.tsx` contains `getHomeState`.
- `apps/wechat_miniprogram/src/pages/today/index.tsx` does not contain `/pages/ai`.
- `npm.cmd run test:plan-flow` includes or is updated to assert question-type weights survive a plan save.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd run typecheck`
- `npm.cmd run test:croc-bti`
- `npm.cmd run test:plan-flow`
- `npm.cmd run test:study-flow`
- `npm.cmd run build:weapp`
</verification>

<must_haves>
- Croc BTI must use existing Flutter names, images, and question/profile source.
- Croc BTI must not default-select unanswered questions.
- Plan edits must not erase Croc-derived question-type weights.
</must_haves>
