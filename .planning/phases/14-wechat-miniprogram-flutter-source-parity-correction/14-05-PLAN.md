---
phase: 14
plan: 05
title: Function-level mini-program rewrite from Flutter gap analysis
status: planned
requirements_addressed: [MOB-01, MOB-02, STUD-02, STUD-03, STUD-04, STUD-05, PLAN-01, PLAN-02, PLAN-03, WRNG-01, WRNG-02, RPT-01]
wave: 1
depends_on: []
---

<objective>
Perform the large corrective rewrite requested after the Flutter and mini-program comparison: every behavioral gap must be fixed at the function boundary where the current mini-program diverges from Flutter/backend source semantics. This plan is the execution entrypoint for Phase 14 and supersedes shallow screenshot-only changes.
</objective>

<line_number_rule>
Line numbers below were captured from the current workspace on 2026-05-21. If earlier edits shift line numbers during execution, use the named function/component as the anchor and apply the same change at the shifted location. Do not treat shifted line numbers as permission to skip the function-level rewrite.
</line_number_rule>

<read_first>
- `docs/wechat/flutter_function_map.md`
- `docs/wechat/miniprogram_flutter_gap_analysis.md`
- `.planning/phases/14-wechat-miniprogram-flutter-source-parity-correction/14-CONTEXT.md`
- `.planning/phases/14-wechat-miniprogram-flutter-source-parity-correction/14-RESEARCH.md`
- `apps/flutter_mobile/lib/features/study_screen.dart`
- `apps/flutter_mobile/lib/sdk/study_client.dart`
- `apps/flutter_mobile/lib/features/today_shell_screen.dart`
- `apps/flutter_mobile/lib/features/plan_screen.dart`
- `apps/flutter_mobile/lib/features/croc_bti_screen.dart`
- `apps/flutter_mobile/lib/features/croc_bti_model.dart`
- `apps/flutter_mobile/lib/features/reports_screen.dart`
- `apps/wechat_miniprogram/src/sdk/index.ts`
- `apps/wechat_miniprogram/src/sdk/client.ts`
- `apps/wechat_miniprogram/src/sdk/httpClient.ts`
- `apps/wechat_miniprogram/src/sdk/transport.ts`
- `apps/wechat_miniprogram/src/sdk/types.ts`
- `apps/wechat_miniprogram/src/sdk/studyChoice.ts`
- `apps/wechat_miniprogram/src/sdk/studyInteraction.ts`
- `apps/wechat_miniprogram/src/sdk/studyFeed.ts`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts`
- `apps/wechat_miniprogram/src/sdk/reportChart.ts`
- `apps/wechat_miniprogram/src/pages/study/index.tsx`
- `apps/wechat_miniprogram/src/pages/study/index.scss`
- `apps/wechat_miniprogram/src/pages/today/index.tsx`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.scss`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx`
- `apps/wechat_miniprogram/src/pages/reports/index.scss`
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx`
- `apps/wechat_miniprogram/src/components/Screen.tsx`
- `apps/wechat_miniprogram/src/pages/account/index.tsx`
</read_first>

<tasks>
<task id="14-05-01">
<title>Make real SDK mode explicit and stop silent mock fallback</title>
<function_targets>
- `apps/wechat_miniprogram/src/sdk/index.ts:4-10`
- `apps/wechat_miniprogram/src/sdk/transport.ts:44-72`
- `apps/wechat_miniprogram/src/sdk/httpClient.ts:43-69`
</function_targets>
<action>
Replace the current silent `shouldUseHttpSdk ? new HttpMiniProgramSdk(...) : new MiniProgramSdk()` selection with an explicit mode gate:
- `TARO_APP_SDK_MODE === 'mock'`: create `new MiniProgramSdk()` and expose a console warning string `Mini Program SDK running in explicit mock mode`.
- `TARO_APP_SDK_MODE === 'http'`: require `TARO_APP_API_BASE_URL` to start with `http://` or `https://`; if missing, throw `Error('TARO_APP_API_BASE_URL is required when TARO_APP_SDK_MODE=http')`.
- unset/other mode: throw `Error('Set TARO_APP_SDK_MODE to http or mock')` in the mini-program build.
In `transport.ts`, add a single 401 refresh/retry path: when an authenticated request returns 401 and refresh token exists, call the refresh route once, save returned tokens, then retry the original request once. Do not retry public auth routes.
In `httpClient.ts`, ensure auth functions continue using public routes and do not recurse through refresh.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/index.ts` contains `TARO_APP_SDK_MODE === 'mock'`.
- `apps/wechat_miniprogram/src/sdk/index.ts` contains `TARO_APP_API_BASE_URL is required when TARO_APP_SDK_MODE=http`.
- `apps/wechat_miniprogram/src/sdk/index.ts` no longer contains `shouldUseHttpSdk ?`.
- `apps/wechat_miniprogram/src/sdk/transport.ts` contains `statusCode === 401`.
- `apps/wechat_miniprogram/src/sdk/transport.ts` contains `refreshSession`.
- `apps/wechat_miniprogram/src/sdk/transport.ts` contains a retry guard equivalent to `hasRetried`.
- `npm.cmd run test:api-contract` exits 0.
</acceptance_criteria>
</task>

<task id="14-05-02">
<title>Rewrite Study mock boundary so real behavior cannot be faked by local correctness</title>
<function_targets>
- `apps/wechat_miniprogram/src/sdk/client.ts:77` `buildQuestionsForSession(mode,total)`
- `apps/wechat_miniprogram/src/sdk/client.ts:94` `isCorrect(question,response)`
- `apps/wechat_miniprogram/src/sdk/client.ts:216` `study.startSession`
- `apps/wechat_miniprogram/src/sdk/client.ts:247` `study.submitAnswer`
- `apps/wechat_miniprogram/src/sdk/client.ts:458` `applyStudySideEffects`
- `apps/wechat_miniprogram/src/sdk/client.ts:498` `applyCompletedSessionToToday`
</function_targets>
<action>
Refactor `MiniProgramSdk` so mock mode is a deterministic fixture only, not a second business implementation:
1. Replace `buildQuestionsForSession(mode,total)` at `client.ts:77` with `buildFixtureQuestionsForSession(mode,total)` that returns a varied fixed bank and throws if it cannot produce the requested total without repeating the same `questionId` or same `word` more than once in a row.
2. Delete `isCorrect(question,response)` at `client.ts:94`. Replace it with `evaluateFixtureAnswer(question,response,skipped)` that exists only inside mock mode and returns a `StudyResult` shaped like backend output. Choice questions must compare only normalized label/value tokens, never accepted Chinese/English meaning text.
3. In `study.startSession` at `client.ts:216`, derive `totalQuestions` from mode target but cap only when `mockStrictFixtureMode` is false. In normal mock tests, newWord must return 20, review 28, mixedTest 34, wrongWordReinforcement 26, rootAffix 4.
4. In `study.submitAnswer` at `client.ts:247`, advance by backend-like `nextIndex = current questionIndex` only after evaluating the submitted current question. Do not update progress if `questionId !== currentQuestion.questionId`; throw `ApiError`-like failure instead.
5. In `applyStudySideEffects` at `client.ts:458`, keep wrong-word/report fixture updates but remove all Today progress and resume mutation.
6. In `applyCompletedSessionToToday` at `client.ts:498`, update Today only from complete session summary and only for the completed mode; incomplete single answers must not change completed task count.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `buildFixtureQuestionsForSession`.
- `apps/wechat_miniprogram/src/sdk/client.ts` no longer contains `function isCorrect`.
- `apps/wechat_miniprogram/src/sdk/client.ts` contains `evaluateFixtureAnswer`.
- `apps/wechat_miniprogram/src/sdk/client.ts` does not contain `acceptedMeanings` inside answer correctness comparison.
- `apps/wechat_miniprogram/src/sdk/client.ts` no longer contains `progress.completedTasks + 1`.
- `apps/wechat_miniprogram/src/sdk/client.ts` contains a guard for `questionId !== question.questionId`.
- `npm.cmd run test:study-flow` asserts 20 newWord fixture questions contain more than one distinct `word`.
- `npm.cmd run test:study-flow` asserts one answer in an incomplete 20-question session does not change Today completed tasks.
</acceptance_criteria>
</task>

<task id="14-05-03">
<title>Rewrite Study page state machine to follow Flutter submit/progress flow</title>
<function_targets>
- `apps/wechat_miniprogram/src/pages/study/index.tsx:59-75` session start `useEffect`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:93-105` current choice/value derivation
- `apps/wechat_miniprogram/src/pages/study/index.tsx:107-130` `applySubmitResponse`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:132-147` `submitQuestion`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:149-164` `markEntryMastered`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:166-182` `acceptDisputedMeaning`
- `apps/wechat_miniprogram/src/sdk/studyFeed.ts` `buildStudyFeedItems`
</function_targets>
<action>
Replace the current Study page update logic with Flutter-equivalent session ownership:
1. At `index.tsx:59-75`, rename the start effect body to `startWithBestAvailableSeed` and make it read `sdk.plan.getActivePlan()` before `sdk.study.startSession`. Pass `mode`, `entrySourceIds`, optional `wordbookId`, and `questionTypeWeightsByMode[mode]`. If route has `resume=1`, use resume hint instead of starting a fresh fixture.
2. At `index.tsx:107-130`, change `applySubmitResponse` so `session.currentQuestion` is replaced only by `answer.currentQuestion`; when `answer.isComplete === true`, set completion and do not preserve the old current question as the next active question.
3. At `index.tsx:132-147`, make `submitQuestion` submit only one of: explicit choice label/value, selected choice label/value, typed input, or empty reveal. Do not submit on empty normal choice first tap.
4. At `index.tsx:149-164`, wrap `markEntryMastered` with `submitting` guard and update session only from SDK response.
5. At `index.tsx:166-182`, allow dispute only when the answered item is incorrect, input-like, and matches the last submitted question id. Do not allow dispute on fresh current question or choice question.
6. In `studyFeed.ts`, make `buildStudyFeedItems` use `answeredQuestions` from session plus `currentQuestion` only when not complete. It must not append stale `lastSubmittedQuestion` if that question is already present in `answeredQuestions`.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `startWithBestAvailableSeed`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `sdk.plan.getActivePlan()`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `answer.isComplete`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` does not contain `currentQuestion: answer.currentQuestion ?? question`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains a dispute guard checking `result.outcome` or `isCorrect`.
- `apps/wechat_miniprogram/src/sdk/studyFeed.ts` contains duplicate prevention by `questionId`.
- `npm.cmd run test:study-flow` asserts completed response does not keep the old question as active current question.
</acceptance_criteria>
</task>

<task id="14-05-04">
<title>Fix choice cleaning, four-option preservation, and question-type rendering</title>
<function_targets>
- `apps/wechat_miniprogram/src/sdk/studyChoice.ts:23` `choiceDisplay`
- `apps/wechat_miniprogram/src/sdk/studyChoice.ts:58` `sanitizeChoices`
- `apps/wechat_miniprogram/src/sdk/studyChoice.ts:76` `resolvedCorrectChoiceTextToken`
- `apps/wechat_miniprogram/src/sdk/studyChoice.ts:131` `choiceState`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:310-311` `choices` and `requiresInput`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:358-380` choice row map
</function_targets>
<action>
Move the option responsibility boundary to match Flutter/Rust:
1. In `studyChoice.ts:58`, keep `sanitizeChoices` for display cleanup but add `validateChoicePayload(question)` that returns `valid: false` when a choice question has fewer than 4 non-empty unique choices after cleanup. Do not silently convert malformed choice questions into input questions.
2. In `studyChoice.ts:23`, ensure `choiceDisplay` preserves backend labels `A/B/C/D` and returns display text separately from submit value.
3. In `pages/study/index.tsx:310-311`, replace `const requiresInput = !choices.length` with `const requiresInput = isInputQuestionType(question.questionType)` using an explicit helper covering Flutter input types. Choice rendering must be chosen by `question.questionType`, not by option array length.
4. In `pages/study/index.tsx:358-380`, render exactly the sanitized four rows for valid choice questions. If invalid, show a contract error view containing `Invalid choice payload` and do not submit.
5. Ensure `ExampleToCnChoice` and `exampleToCnChoiceNoTranslation` display Chinese meaning options from backend payload. Do not generate English distractor text in the page layer.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/studyChoice.ts` contains `validateChoicePayload`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `isInputQuestionType`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` no longer contains `const requiresInput = !choices.length`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `Invalid choice payload`.
- `npm.cmd run test:study-flow` asserts a valid four-choice question renders four choices.
- `npm.cmd run test:study-flow` asserts a duplicate-choice payload is rejected instead of being rendered as three options.
- `npm.cmd run test:study-flow` asserts `exampleToCnChoice` fixture choices are Chinese meanings, not English sentence completions.
</acceptance_criteria>
</task>

<task id="14-05-05">
<title>Fix first-tap, double-tap, row feedback, and Study side icon behavior</title>
<function_targets>
- `apps/wechat_miniprogram/src/sdk/studyInteraction.ts:1` `shouldSubmitChoiceTap`
- `apps/wechat_miniprogram/src/sdk/studyInteraction.ts:10` `handleStudyChoiceTap`
- `apps/wechat_miniprogram/src/pages/study/index.tsx:238-252` choice tap integration
- `apps/wechat_miniprogram/src/pages/study/index.tsx:334-342` side actions
- `apps/wechat_miniprogram/src/pages/study/index.tsx:343-382` answer panel / choice rows
- `apps/wechat_miniprogram/src/pages/study/index.scss` choice/side-action styles
</function_targets>
<action>
Implement Flutter's visible interaction exactly:
1. In `studyInteraction.ts:1`, `shouldSubmitChoiceTap` must require same `questionId`, same choice value, elapsed time `< 420`, and `previous.submitted !== true`. Remove any rule where slow second tap of selected choice submits.
2. In `handleStudyChoiceTap` at `studyInteraction.ts:10`, return `{ selected, shouldSubmit }`; first tap always returns `shouldSubmit: false`.
3. At `pages/study/index.tsx:238-252`, reset `lastChoiceTapRef` after every successful submit and whenever Swiper page/question changes.
4. At `pages/study/index.tsx:343-382`, remove separate choice-result cards. Choice answer feedback must be row-level: correct row green, selected wrong row red, neutral rows unchanged; add small check/cancel markers inside rows.
5. At `pages/study/index.tsx:334-342`, replace text side action labels with a `StudyIcon` component or copied icon assets for home, close, lightbulb, comment, visibility, gavel, delete. Buttons must expose disabled state, not text fallbacks.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/studyInteraction.ts` contains `questionId`.
- `apps/wechat_miniprogram/src/sdk/studyInteraction.ts` no longer contains `selected === choice`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `lastChoiceTapRef.current = undefined`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `choice--correct`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `choice--wrong`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `StudyIcon`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` no longer contains the mojibake side-action glyph strings currently rendered as labels.
- `npm.cmd run test:study-flow` asserts first tap selects only and double tap submits.
- `npm.cmd run test:ui-contract` asserts Study side actions are icon-based.
</acceptance_criteria>
</task>

<task id="14-05-06">
<title>Replace global mojibake constants with UTF-8 Flutter-derived text tables</title>
<function_targets>
- `apps/wechat_miniprogram/src/components/Screen.tsx:16` `tabs`
- `apps/wechat_miniprogram/src/components/Screen.tsx:24` `drawerItems`
- `apps/wechat_miniprogram/src/pages/today/index.tsx:68-111` hero/task rows
- `apps/wechat_miniprogram/src/pages/study/index.tsx:22-42` `questionTypeLabels`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx:13-23` labels/meta
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx:14-18` filters
- `apps/wechat_miniprogram/src/pages/reports/index.tsx:17-23` `modeNames`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx:41-56` labels
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:34-103` questions/profile text/profile visuals
</function_targets>
<action>
Create `apps/wechat_miniprogram/src/sdk/flutterText.ts` or page-specific UTF-8 constants and replace all mojibake user-visible strings in the targeted functions/constants. Use Flutter source text where available:
- bottom nav: `今日`, `计划`, `错词`, `报告`
- drawer: `新手引导`, `鳄bti 学习人格`, `个人信息`, `排行榜`, `设置`, `Check for updates`, `退出登录`
- study question labels: `根据例句选择中文释义`, `选择最佳释义`, `拼写单词`, `输入中文释义`, and Flutter-equivalent labels for existing question types
- Today task names: `新词学习`, `复习`, `混合测试`, `错词强化`
- Reports mode names: `新词学习`, `复习`, `混合测试`, `错词强化`, `词根词缀`
- Croc BTI questions/profile titles/summaries copied from Flutter source in UTF-8.
Do not manually guess damaged strings when a Flutter source constant exists.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/components/Screen.tsx` contains `今日`.
- `apps/wechat_miniprogram/src/components/Screen.tsx` contains `鳄bti 学习人格`.
- `apps/wechat_miniprogram/src/pages/study/index.tsx` contains `根据例句选择中文释义`.
- `apps/wechat_miniprogram/src/pages/today/index.tsx` contains `新词学习`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `错词强化`.
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains `VINA`.
- `apps/wechat_miniprogram/src` has no occurrences of `æ`, `éˆ`, `閳`, or `å®` in `.ts` or `.tsx` files, except comments explicitly documenting encoding tests.
- `npm.cmd run test:ui-contract` exits 0.
</acceptance_criteria>
</task>

<task id="14-05-07">
<title>Fix Croc BTI source constants, answer scale, result gating, and apply sequence</title>
<function_targets>
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:34` `crocBtiQuestions`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:68` `profileText`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:87` `profileVisuals`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:106` `clampCrocBtiAnswer`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:110` `hasCompleteCrocBtiAnswers`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:114` `evaluateCrocBti`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:222` `rebalanceCrocBtiQuestionTypeWeights`
- `apps/wechat_miniprogram/src/sdk/crocBti.ts:280` `scoreAxis`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx:26` `restoreCompleteAnswers`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx:99` `updateAnswer`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx:103` `showResultOrHint`
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx:124` `applyResult`
</function_targets>
<action>
Port Croc BTI from Flutter source rather than current approximated TypeScript:
1. Replace `crocBtiQuestions`, `profileText`, and `profileVisuals` with generated UTF-8 constants copied from Flutter `croc_bti_model.dart`. Preserve all existing asset filenames under `src/assets/croc_bti`.
2. If Flutter uses 1/2/3 segmented answers, replace `CrocBtiAnswer = -2|-1|0|1|2` with the exact Flutter answer domain and update `clampCrocBtiAnswer` and `scoreAxis`. If Flutter source confirms 5-point answers, keep 5-point but update the UI to match Flutter labels and selected state. The executor must cite the exact Flutter function/constant in the summary.
3. `restoreCompleteAnswers` must restore partial answers as partial UI state, but `showResult` must remain false until `hasCompleteCrocBtiAnswers` is true.
4. `showResultOrHint` must disable or toast without showing result when incomplete; when complete, save versioned answers and set result view.
5. `applyResult` must call, in order: save answers, save minutes, `sdk.crocBti.saveProfile`, `sdk.plan.savePlan`, `sdk.plan.applySavedPlanToToday`, `sdk.sync.flushPendingToCloud`, then navigate/refresh.
6. Result card CSS must use image height 180px, separate code/title lines, centered summary, no inline `result.code}{result.title`.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/crocBti.ts` contains all 16 Flutter Croc BTI profile codes.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` no longer contains `demo-user` in storage key names; keys are scoped by actual account id or a documented anonymous id helper.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `hasCompleteCrocBtiAnswers`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.tsx` contains `flushPendingToCloud`.
- `apps/wechat_miniprogram/src/pages/croc-bti/index.scss` contains `height: 180px`.
- `npm.cmd run test:croc-bti` asserts fresh state has no selected answers.
- `npm.cmd run test:croc-bti` asserts incomplete answers cannot show result.
- `npm.cmd run test:croc-bti` asserts apply call order.
- `npm.cmd run test:croc-bti` asserts each profile asset path exists.
</acceptance_criteria>
</task>

<task id="14-05-08">
<title>Fix Today and Plan synchronization boundaries</title>
<function_targets>
- `apps/wechat_miniprogram/src/pages/today/index.tsx:49` `TodayPage`
- `apps/wechat_miniprogram/src/pages/today/index.tsx:58-62` progress/completion calculation
- `apps/wechat_miniprogram/src/pages/today/index.tsx:94-105` task rows
- `apps/wechat_miniprogram/src/pages/plan/index.tsx:51-52` `planInput` and `savedInput`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx:61` `persistPlanAndWordbook`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx:74` `savePlan`
- `apps/wechat_miniprogram/src/pages/plan/index.tsx:84` `applySavedPlanToToday`
- `apps/wechat_miniprogram/src/sdk/planFlow.ts:15` `buildPlanInput`
</function_targets>
<action>
Make Today and Plan match Flutter's state ownership:
1. In `TodayPage`, replace hero completion with a helper `calculateTodayCompletion(today)` that uses backend `dailyProgress` and per-mode task state. Do not infer completion from plan target alone.
2. Task rows must use actual per-mode completed/total values from today state when available; fallback to plan target only for total display when backend state is absent.
3. On returning from study, plan apply, or reward claim, reload via `sdk.today.getHomeState()` before rendering completion.
4. In `buildPlanInput`, preserve `questionTypeWeightsByMode`, `growthRuleMode`, `sharedGrowthRule`, `growthRulesByMode`, active wordbook selection, and all Croc-derived fields when the user edits counts.
5. In `persistPlanAndWordbook`, perform `toggleWordbook` before `savePlan`, then refetch wordbooks and active plan.
6. In `applySavedPlanToToday`, do not mutate local Today state directly; navigate to Today with a refresh flag or call the Today refresh route.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/today/index.tsx` contains `calculateTodayCompletion`.
- `apps/wechat_miniprogram/src/pages/today/index.tsx` contains `sdk.today.getHomeState()`.
- `apps/wechat_miniprogram/src/pages/today/index.tsx` no longer uses `heroTarget` as completion denominator.
- `apps/wechat_miniprogram/src/sdk/planFlow.ts` contains `questionTypeWeightsByMode`.
- `apps/wechat_miniprogram/src/sdk/planFlow.ts` contains `growthRulesByMode`.
- `apps/wechat_miniprogram/src/pages/plan/index.tsx` contains `toggleWordbook`.
- `npm.cmd run test:plan-flow` asserts question-type weights survive a count-only edit.
- `npm.cmd run test:study-flow` asserts Today refresh after complete session uses SDK state.
</acceptance_criteria>
</task>

<task id="14-05-09">
<title>Fix Reports chart constants, selection, and backend data usage</title>
<function_targets>
- `apps/wechat_miniprogram/src/sdk/reportChart.ts:5-13` constants
- `apps/wechat_miniprogram/src/sdk/reportChart.ts:15` `reportAccuracy`
- `apps/wechat_miniprogram/src/sdk/reportChart.ts:29` `reportAccuracyColor`
- `apps/wechat_miniprogram/src/sdk/reportChart.ts:35` `buildReportChartPoints`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx:44` `Chart`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx:114` `ReportsPage`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx:127-135` overview load/selection
- `apps/wechat_miniprogram/src/pages/reports/index.tsx:182-207` mode card map
</function_targets>
<action>
Rewrite Reports chart to use Flutter-equivalent data and selection:
1. In `reportChart.ts:5-13`, set constants to `STEP_X=56`, `EDGE_INSET=28`, `LEFT_GUTTER=34`, `RIGHT_GUTTER=18`, `TOP_GUTTER=12`, `BOTTOM_GUTTER=22`, normal height `140`, compact height `104`, touch size `24`.
2. In `buildReportChartPoints`, preserve stable point positions for scroll width and return enough metadata for labels, selected state, and compact sizing.
3. In `ReportsPage`, default selected day to the last `dailySeries` item after load. Do not use mock fallback if HTTP mode is configured and request fails.
4. In `Chart`, point click must call `onSelect(item)` for the exact point tapped, and the selected card must update to that date.
5. In mode cards, clicking a mode sets `selectedMode`; selected card renders `reports.modeSeries[mode]` in compact chart and can be toggled/changed without losing daily selected day.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/reportChart.ts` contains `REPORT_CHART_HEIGHT = 140`.
- `apps/wechat_miniprogram/src/sdk/reportChart.ts` contains `REPORT_CHART_COMPACT_HEIGHT = 104`.
- `apps/wechat_miniprogram/src/sdk/reportChart.ts` contains `REPORT_CHART_TOUCH_SIZE = 24`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `setSelectedDay(next.dailySeries[next.dailySeries.length - 1])`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `reports.modeSeries[mode.mode]`.
- `npm.cmd run test:reports` asserts daily point selection changes selected detail.
- `npm.cmd run test:reports` asserts compact mode chart height is 104.
</acceptance_criteria>
</task>

<task id="14-05-10">
<title>Fix Wrong Words, drawer/account navigation, and Flutter icon/resource mapping</title>
<function_targets>
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx:21` `WrongWordsPage`
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx:29` `filteredWords`
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx:80` wrong-word reinforcement navigation
- `apps/wechat_miniprogram/src/components/Screen.tsx:16` `tabs`
- `apps/wechat_miniprogram/src/components/Screen.tsx:23` `AccountDrawer`
- `apps/wechat_miniprogram/src/components/Screen.tsx:86` `BottomNav`
- `apps/wechat_miniprogram/src/pages/account/index.tsx:76` drawer plan action
</function_targets>
<action>
Complete the non-Study parity breaks exposed by the comparison:
1. In `WrongWordsPage`, add detail expansion state keyed by `entrySourceId`; on row click, call `sdk.wrongWords.getDetail(entrySourceId)` and render hint, recent mistakes, examples, roots/affixes when returned. Do not hardcode root-affix counts.
2. When starting wrong-word reinforcement from filtered list, pass a serialized filter or `entrySourceIds` query payload so Study can ask backend for the intended pool. Do not always start the same generic `wrongWordReinforcement` pool.
3. In `Screen.tsx`, replace tab/drawer text glyph icons with `AppIcon` mapping for Flutter Material names: bottom tabs `today`, `tune`, `menu_book`, `query_stats`; drawer `emoji_objects`, `psychology`, `account_circle`, `leaderboard`, `settings`, `system_update_alt`, `logout`.
4. Keep AI absent from bottom tabs, app config, and drawer.
5. In `account/index.tsx:76`, replace `Taro.switchTab({ url: '/pages/plan/index' })` with `Taro.navigateTo` or `Taro.redirectTo` consistent with custom navigation.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx` contains `getDetail`.
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx` contains `entrySourceIds`.
- `apps/wechat_miniprogram/src/pages/wrong-words/index.tsx` no longer contains hardcoded root-affix metric text for `48` unless it comes from data.
- `apps/wechat_miniprogram/src/components/Screen.tsx` contains `AppIcon`.
- `apps/wechat_miniprogram/src/components/Screen.tsx` does not contain `AI`.
- `apps/wechat_miniprogram/src/pages/account/index.tsx` no longer contains `Taro.switchTab`.
- `npm.cmd run test:ui-contract` asserts Account is not a bottom tab and Croc BTI/Leaderboard are drawer entries.
</acceptance_criteria>
</task>

<task id="14-05-11">
<title>Add function-level regression tests before and after rewrite</title>
<function_targets>
- `apps/wechat_miniprogram/package.json`
- test files behind `npm.cmd run test:study-flow`
- test files behind `npm.cmd run test:croc-bti`
- test files behind `npm.cmd run test:reports`
- test files behind `npm.cmd run test:plan-flow`
- test files behind `npm.cmd run test:ui-contract`
- test files behind `npm.cmd run test:api-contract`
</function_targets>
<action>
Add or update focused tests so each rewritten function has a direct guard:
- SDK mode: explicit `mock`, explicit `http`, and missing URL failure.
- Study fixture generation: no one-word repetition; 20/28/34/26/4 targets available.
- Study tap interaction: first tap select only, double tap submit, question id reset prevents accidental submit.
- Study payload validation: duplicate choice becomes contract error, not 3-choice UI.
- Study submit: wrong question id fails; incomplete answer does not mutate Today completed tasks.
- Croc BTI: no default selected answers, incomplete cannot show result, complete can show result, asset paths exist, apply call order is correct.
- Plan: count-only edits preserve `questionTypeWeightsByMode` and growth fields.
- Reports: last day selected by default, point tap updates detail, mode compact chart uses height 104.
- UI contract: no mojibake, no AI route/tab, no text glyph study side actions, drawer contains Croc BTI and leaderboard.
</action>
<acceptance_criteria>
- `npm.cmd run test:api-contract` exits 0.
- `npm.cmd run test:study-flow` exits 0.
- `npm.cmd run test:croc-bti` exits 0.
- `npm.cmd run test:reports` exits 0.
- `npm.cmd run test:plan-flow` exits 0.
- `npm.cmd run test:ui-contract` exits 0.
- `npm.cmd run typecheck` exits 0.
- `npm.cmd run build:weapp` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd run typecheck`
- `npm.cmd run test:api-contract`
- `npm.cmd run test:study-flow`
- `npm.cmd run test:croc-bti`
- `npm.cmd run test:reports`
- `npm.cmd run test:plan-flow`
- `npm.cmd run test:ui-contract`
- `npm.cmd run build:weapp`
</verification>

<must_haves>
- No real mini-program path may silently fall back to mock.
- Study must use backend/source session semantics: render by `questionType`, preserve four options, submit only through SDK, and update progress only from SDK response.
- Croc BTI must use Flutter source constants, profile assets, answer domain, result gating, and apply sequence.
- Today and Plan must preserve backend/Rust ownership of progress, task counts, plan coverage, and question-type weights.
- Reports charts must be selectable and sized from Flutter-equivalent constants.
- Mojibake text, text glyph icons, and AI mini-program UI must be absent.
</must_haves>
