---
phase: 14
plan: 04
title: Correct Reports chart selection and add source-parity regression gates
status: planned
requirements_addressed: [MOB-01, MOB-02, RPT-01, STUD-04]
wave: 3
depends_on: [14-01, 14-02, 14-03]
---

<objective>
Bring Reports chart behavior back to Flutter source parity and lock the repaired Study/Croc/Plan/Today behavior with focused regression gates.
</objective>

<read_first>
- `.planning/phases/14-wechat-miniprogram-flutter-source-parity-correction/14-CONTEXT.md`
- `.planning/phases/14-wechat-miniprogram-flutter-source-parity-correction/14-RESEARCH.md`
- `apps/flutter_mobile/lib/features/reports_screen.dart`
- `apps/flutter_mobile/lib/sdk/reports_client.dart`
- `apps/wechat_miniprogram/src/sdk/reportChart.ts`
- `apps/wechat_miniprogram/src/sdk/types.ts`
- `apps/wechat_miniprogram/src/sdk/client.ts`
- `apps/wechat_miniprogram/src/pages/reports/index.tsx`
- `apps/wechat_miniprogram/src/pages/reports/index.scss`
- `apps/wechat_miniprogram/package.json`
</read_first>

<tasks>
<task id="14-04-01">
<title>Match Reports chart data and selection model</title>
<action>
Ensure `ReportsOverview` includes `dailySeries`, `modeBreakdown`, and `modeSeries`. On load, default selected day to the last `dailySeries` item and selected mode to the first `modeBreakdown` item. Daily point tap must update the selected detail card. Mode card tap must toggle selected mode and show a compact chart from `modeSeries[mode]`.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/types.ts` contains `modeSeries`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `setSelectedDay`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `setSelectedMode`.
- `apps/wechat_miniprogram/src/pages/reports/index.tsx` contains `modeSeries`.
- `npm.cmd run test:reports` includes or is updated to assert default selected day is the last daily-series item.
- `npm.cmd run test:reports` includes or is updated to assert mode card selection reads `modeSeries[mode]`.
</acceptance_criteria>
</task>

<task id="14-04-02">
<title>Match Flutter chart dimensions and point hitboxes</title>
<action>
Update chart helper and SCSS to match Flutter constants:
- `stepX = 56`
- `edgeInset = 28`
- `leftGutter = 34`
- `rightGutter = 18`
- `topGutter = 12`
- `bottomGutter = 22`
- normal `chartHeight = 140`
- compact `chartHeight = 104`
- point hitbox 24x24
- visual node 12px normal, 10px compact
Keep red/orange/green accuracy colors at `<50`, `<85`, otherwise green, and selected point white border.
</action>
<acceptance_criteria>
- `apps/wechat_miniprogram/src/sdk/reportChart.ts` contains `stepX: 56` or `STEP_X = 56`.
- `apps/wechat_miniprogram/src/sdk/reportChart.ts` contains `leftGutter: 34` or `LEFT_GUTTER = 34`.
- `apps/wechat_miniprogram/src/sdk/reportChart.ts` contains `chartHeight`.
- `apps/wechat_miniprogram/src/pages/reports/index.scss` contains `24px`.
- `apps/wechat_miniprogram/src/pages/reports/index.scss` contains `12px`.
- `apps/wechat_miniprogram/src/pages/reports/index.scss` contains `10px`.
- `npm.cmd run test:reports` includes or is updated to assert compact chart height is 104.
</acceptance_criteria>
</task>

<task id="14-04-03">
<title>Add source-parity contract tests for user-reported regressions</title>
<action>
Extend focused tests so this exact set of regressions cannot return:
- Croc BTI result card does not inline code and title.
- Croc BTI fresh answers are empty, not default-selected.
- Study `newWord` session target 20 has more than one distinct word.
- Study choice questions preserve four choices when four valid choices are present.
- Study first tap selects only; double tap submits.
- Study wrong answer row feedback marks selected wrong red and correct green.
- Today completed tasks do not increment after one answer in an incomplete session.
- Reports daily point selection changes selected detail.
- Reports mode card selection toggles compact chart.
</action>
<acceptance_criteria>
- `npm.cmd run test:study-flow` passes.
- `npm.cmd run test:croc-bti` passes.
- `npm.cmd run test:reports` passes.
- `npm.cmd run test:plan-flow` passes.
- `npm.cmd run test:ui-contract` passes.
</acceptance_criteria>
</task>

<task id="14-04-04">
<title>Run full mini-program verification without AI UI regression</title>
<action>
Run all focused checks and the WeChat build. Confirm AI UI remains omitted from mini-program routes and bottom nav.
</action>
<acceptance_criteria>
- `npm.cmd run typecheck` exits 0.
- `npm.cmd run test:study-flow` exits 0.
- `npm.cmd run test:croc-bti` exits 0.
- `npm.cmd run test:reports` exits 0.
- `npm.cmd run test:plan-flow` exits 0.
- `npm.cmd run test:ui-contract` exits 0.
- `npm.cmd run build:weapp` exits 0.
- `apps/wechat_miniprogram/src/components/Screen.tsx` does not contain `AI`.
- `apps/wechat_miniprogram/src/app.config.ts` does not contain `/pages/ai`.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd run typecheck`
- `npm.cmd run test:study-flow`
- `npm.cmd run test:croc-bti`
- `npm.cmd run test:reports`
- `npm.cmd run test:plan-flow`
- `npm.cmd run test:ui-contract`
- `npm.cmd run build:weapp`
</verification>

<must_haves>
- Reports charts must be selectable and compact where Flutter is compact.
- Regression tests must directly cover the bugs reported in WeChat DevTools screenshots.
- AI UI must remain absent.
</must_haves>
