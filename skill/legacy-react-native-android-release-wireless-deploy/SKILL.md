---
name: legacy-react-native-android-release-wireless-deploy
description: Historical React Native Android cached release deploy workflow. Use only when intentionally working on legacy apps/mobile Android cached-build references; prefer flutter-android-release-wireless-deploy for current Flutter delivery.
---

# Legacy React Native Android Release Wireless Deploy

This skill preserves the old React Native cached Android build workflow for historical reference.

Do not use it for current mobile delivery unless the user explicitly asks to work on the legacy React Native app under `apps/mobile`.

For current Flutter release installation, use:

- `skill/flutter-android-release-wireless-deploy/`

For the legacy workflow details, read:

- `workflow.md`
- `troubleshooting.md`
